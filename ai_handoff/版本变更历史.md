# DGWEB — 版本变更历史（已定格版本，由管理器自动汇总）
最后更新：2026-08-07 08:11

> 说明：每次「定格版本」后，点击「理解AI」会自动重新汇总全部已定格版本的改动。
> AI 阅读时请按版本号从旧到新浏览，关注最新定格改动的功能与实现。

## DGWEB_V0.02  （2026-08-01 22:41）

- 手动触发验证一次吧
- [attempt 1] HTTP 412 / [attempt 2] HTTP 412
- 行，把项目文件夹命名V0.01，之后每次改动都复制一个副本命名都+0.01，同时每个版本都生成一个说明文件标注做了哪些改动
- 每次修改项目先创建新版本直到我说没问题停止新版本定格，确认实现后下次增加功能或修改再复制一次项目修改
- Continue if you have next steps, or stop and ask for clarification if you are unsure how to proceed.
- 定格版本时根据opencode记录生成版本说明，同时把目前的交流记录整理一下写一份AI衔接进度文档，方便其他AI理解项目

## DGWEB_V0.06  （2026-08-02 01:15）

- 数据源升级：视频列表从 arc/search 接口（硬限制最新 180 条）改为 search/type 搜索接口
  （关键词=UP主名"低配挂机研讨会"，按发布翻页 12 页，过滤 mid），成功抓取全量 512 个视频
  （archive_count 528 含不可见的已删/审核中稿件）。
- 新增 SESSDATA 登录 Cookie 支持（本地使用，不上传），buvid3/4 + SESSDATA 即可访问搜索接口，
  显著降低 B站 412 风控概率。
- 412 快速失败策略：每页最多重试 3 次（等待 5s/10s/20s），遇 412 立即停止；
  stats 统计优先更新，抓取失败时复用本地缓存。
- 统计第三项从 PLAYLISTS（合集数，接口 404 无法获取）改为 LIKES（获赞数，card 接口 like_num）。
- index.html 统计元素改为 statLikes / LIKES，applyStats 映射 stats.likes。
- HTML 标签清理：新增 clean_html() 剥离搜索接口返回的 <em class="keyword"> 高亮标签（363 条受影响），
  避免前端显示异常。
- 缓存兜底：新增 load_cache()，视频或统计获取失败时自动复用本地 latest.json 上次成功数据。
- 数据托管升级：latest.json 同步推送 bili-sync（CDN）和 DGWEB 仓库（本站托管），
  前端 DATA_URLS 多源 fallback：本站 /latest.json → jsDelivr CDN → GitHub raw。
- 数据缩量防护：抓取结果与旧缓存按 link 合并去重取并集，避免搜索接口分页抖动导致数据缩量。
- 本次实测数据：512 视频（2023-09 至 2026-08），fans≈191900 / videos=528 / likes≈1178600。

## DGWEB_V0.08  （2026-08-03 11:19）

- 手动触发验证一次吧
- [attempt 1] HTTP 412 / [attempt 2] HTTP 412
- 行，把项目文件夹命名V0.01，之后每次改动都复制一个副本命名都+0.01，同时每个版本都生成一个说明文件标注做了哪些改动
- 每次修改项目先创建新版本直到我说没问题停止新版本定格，确认实现后下次增加功能或修改再复制一次项目修改
- Continue if you have next steps, or stop and ask for clarification if you are unsure how to proceed.
- 定格版本时根据opencode记录生成版本说明，同时把目前的交流记录整理一下写一份AI衔接进度文档，方便其他AI理解项目

## DGWEB_V0.09  （2026-08-03 20:48）

- 手动触发验证一次吧
- [attempt 1] HTTP 412 / [attempt 2] HTTP 412
- 行，把项目文件夹命名V0.01，之后每次改动都复制一个副本命名都+0.01，同时每个版本都生成一个说明文件标注做了哪些改动
- 每次修改项目先创建新版本直到我说没问题停止新版本定格，确认实现后下次增加功能或修改再复制一次项目修改
- Continue if you have next steps, or stop and ask for clarification if you are unsure how to proceed.
- 定格版本时根据opencode记录生成版本说明，同时把目前的交流记录整理一下写一份AI衔接进度文档，方便其他AI理解项目

## DGWEB_V0.10  （2026-08-03 23:34）

- 手动触发验证一次吧
- [attempt 1] HTTP 412 / [attempt 2] HTTP 412
- 行，把项目文件夹命名V0.01，之后每次改动都复制一个副本命名都+0.01，同时每个版本都生成一个说明文件标注做了哪些改动
- 每次修改项目先创建新版本直到我说没问题停止新版本定格，确认实现后下次增加功能或修改再复制一次项目修改
- Continue if you have next steps, or stop and ask for clarification if you are unsure how to proceed.
- 定格版本时根据opencode记录生成版本说明，同时把目前的交流记录整理一下写一份AI衔接进度文档，方便其他AI理解项目

## DGWEB_V0.11  （2026-08-04 14:44）

- 手动触发验证一次吧
- [attempt 1] HTTP 412 / [attempt 2] HTTP 412
- 行，把项目文件夹命名V0.01，之后每次改动都复制一个副本命名都+0.01，同时每个版本都生成一个说明文件标注做了哪些改动
- 每次修改项目先创建新版本直到我说没问题停止新版本定格，确认实现后下次增加功能或修改再复制一次项目修改
- Continue if you have next steps, or stop and ask for clarification if you are unsure how to proceed.
- 定格版本时根据opencode记录生成版本说明，同时把目前的交流记录整理一下写一份AI衔接进度文档，方便其他AI理解项目

## DGWEB_V0.12  （2026-08-05 13:52）

- 手动触发验证一次吧
- [attempt 1] HTTP 412 / [attempt 2] HTTP 412
- 行，把项目文件夹命名V0.01，之后每次改动都复制一个副本命名都+0.01，同时每个版本都生成一个说明文件标注做了哪些改动
- 每次修改项目先创建新版本直到我说没问题停止新版本定格，确认实现后下次增加功能或修改再复制一次项目修改
- Continue if you have next steps, or stop and ask for clarification if you are unsure how to proceed.
- 定格版本时根据opencode记录生成版本说明，同时把目前的交流记录整理一下写一份AI衔接进度文档，方便其他AI理解项目

## DGWEB_V0.14  （2026-08-05 14:08）

- 手动触发验证一次吧
- [attempt 1] HTTP 412 / [attempt 2] HTTP 412
- 行，把项目文件夹命名V0.01，之后每次改动都复制一个副本命名都+0.01，同时每个版本都生成一个说明文件标注做了哪些改动
- 每次修改项目先创建新版本直到我说没问题停止新版本定格，确认实现后下次增加功能或修改再复制一次项目修改
- Continue if you have next steps, or stop and ask for clarification if you are unsure how to proceed.
- 定格版本时根据opencode记录生成版本说明，同时把目前的交流记录整理一下写一份AI衔接进度文档，方便其他AI理解项目

## DGWEB_V0.15  （2026-08-05 16:59）

- 手动触发验证一次吧
- [attempt 1] HTTP 412 / [attempt 2] HTTP 412
- 行，把项目文件夹命名V0.01，之后每次改动都复制一个副本命名都+0.01，同时每个版本都生成一个说明文件标注做了哪些改动
- 每次修改项目先创建新版本直到我说没问题停止新版本定格，确认实现后下次增加功能或修改再复制一次项目修改
- Continue if you have next steps, or stop and ask for clarification if you are unsure how to proceed.
- 定格版本时根据opencode记录生成版本说明，同时把目前的交流记录整理一下写一份AI衔接进度文档，方便其他AI理解项目

## DGWEB_V0.16  （2026-08-05 16:59）

- 手动触发验证一次吧
- [attempt 1] HTTP 412 / [attempt 2] HTTP 412
- 行，把项目文件夹命名V0.01，之后每次改动都复制一个副本命名都+0.01，同时每个版本都生成一个说明文件标注做了哪些改动
- 每次修改项目先创建新版本直到我说没问题停止新版本定格，确认实现后下次增加功能或修改再复制一次项目修改
- Continue if you have next steps, or stop and ask for clarification if you are unsure how to proceed.
- 定格版本时根据opencode记录生成版本说明，同时把目前的交流记录整理一下写一份AI衔接进度文档，方便其他AI理解项目

## DGWEB_V0.17  （2026-08-05 21:30）

1. 修复活动关卡 EX/S/MO/S 关卡编号缺失：视频标题分支中 EX/MO/S 关卡（如 AS-EX-1、TA-MO-1、FM-S-1）无法从标题解析出编号，全程提取按 SEE"普通编号"处理。已修复 _build_modes.py 的 CODE_RE、源码截止段（含-1 支持），normalize_code 统一 AS-EX-1 式规范，EX/S/MO/S 分支对齐。
2. stage.html 的 matchCode / normCode 同步更新（normalizeCode），匹配前 toUpperCase 兼容 cv-ex-4 例外。
3. modes.json 重新聚合：event-stages 401/ESW-705 关（EX 260 / MO 8 / S 38），ESW 的 S 9 坐，其他模块 segment 的内容保持；阶段排序统一「普通→EX→S→MO」。
4. 关卡代码统一为「普通→EX→S→MO」排序（含关卡代码排序 AS-1~9、AS-EX-1~8、AS-S-1~4、AS-MO-1，_build_modes.py 的 stage_sort_key 实现）。
5. 新增 9 个覆盖场景测试（TD-EX-1 等），live 验证 714 个 chip 全部匹配。
6. 全链路回归验证：本地 714 关 chip、线上 EX/MO/S 关卡代码与视频对应关系全部匹配。
7. 修复导航<nav> 打开与闭合标签丢失导致的页面布局错乱（fixed top-0 层级、max-w-[1400px] 内容居中）
8. 关卡视频方案改版：theme-videos.html 改为按视频实际标题（含模式/地图）逐条匹配关卡，替代原来的整体对应方案；支持对主模式/活动/情景逐帧分析；模板页全部重写 URL。
9. 视频方案扩至全模式：所有模式关卡支持 — 关卡/关卡/BGM 全会话标签，活动/模式变化；13 个模式页 + 主题视频页联合行走。
10. 关卡视频方案扩至其他活动：其他活动模式下（纷争演绎、其它）全文并入 other-events，事件 2023 已复活且专区布局不复返；活动页专门区分 3 种专区类型；活动事件 2023 组 8 个；activity-in 逻辑修复（inv 切换老 bug）。
11. 「干员泛用榜」改名为「核心使用率」：ranking.html 的标题/H1/列表标题/动态标题（嵌套「使用率」）。全站 20 页导航与 footer 同步更新文案。
12. 全站移动端适配（手机/平板）：新增番茄盘移动菜单，<768px 显示汉堡按钮展开并排下拉菜单；320px 视口无横向滚动；程序化嵌入所有 20 个页面（源码锚点对齐、媒体查询注入最后 <style>、.nav-toggle 兼容 Tailwind md:hidden）；ranking 表格响应式压缩（320px 无溢出）；aliases 头像图 120px；detail/stage 表单响应式；修复 index 模式化移动端按钮体验。
13. 首页统计数字即时显示与动效优化：
    - 修复每次打开时统计数字按旧值滚动、需滚动才更新为最新：IntersectionObserver 在统计数据返回前触发动画的问题，新增 statsReady/heroRevealed 标记，数据就绪后再播（本地+线上验证）。
    - 动画期间统计框宽度固定（预测量最终文本宽度，数字在框内居中跳动）；高度同样测量后固定，nowrap 防换行（实测 169x58/100x58/202x58 恒定，手机无横溢）。
    - 统计标签补充中文：FOLLOWERS 前加「关注」、VIDEOS 前加「视频」、LIKES 前加「点赞」（.cn 类重置字母间距）。
    - 解除 LIKES 单元格 hidden sm:block 限制，手机端也显示点赞数据。
    - 修复老内核（搜狗等）不支持 AbortSignal.timeout 导致数据为 0：新增 fetchWithTimeout 逐级兼容（AbortSignal.timeout → AbortController+setTimeout → 无超时），并缓存 stats 回退。
14. 白昼/暗夜主题：
    - 全站 20 页注入右下角圆形切换按钮（月/日图标随主题，hover 旋转）。
    - html.light 规则覆盖 CSS 变量 + 全部 Tailwind 色值 utility（含透明度变体），并覆盖滚动条/噪声/加载屏/cursor 等特殊元素。
    - 切换动画：点击处同心圆扩散（theme-overlay + themeWipe keyframes，550ms 兜底 900ms）。
    - localStorage 记忆主题（dgweb_theme）+ head 防闪烁脚本。
    - 图标语义修正：按钮显示切换目标（暗夜显太阳、白昼显月亮）。
    - 悬停 title 同步联动（暗夜显示「白昼模式」、白昼显示「黑夜模式」）。

15. 新增干员别称：伊内丝→ins、星熊→熊大、斩业星熊→熊二、予愿安洁莉娜→茅圆；同步修改 aliases.html 的 ALIAS_OPS 与 ranking.html 的视频标题归并映射 ALIAS，本地/线上搜索 4 项均正常命中。

16. 彩蛋：一分钟内切换夜晚/白晐主题 4 次（全站 20 页任意页面）跳转 easter-egg.html。彩蛋页：上黑/中深红（#7a0c0c）/下黑三段布局，中红区中心浮现「是否确认关闭全舰防御系统？」，黑色区域每秒随机出现白色省略号；文字出现 4 秒后点击显示「是/否」，选择后屏幕变黑——选是显示「做得好，预言家。」，选否显示「谢谢你，博士」，7 秒后自动返回首页。本地/线上全流程验证通过。（迭代）选项↓移至 62%处；省略号改为常驻不销毁（每秒累积）；触发时播放《Storyteller》（塞居唱片，《巴别塔OST》首曲，收集直接下载转 192k mp3 自托管 easter-storyteller.mp3，autoplay 被拒时首次点击触发）。本地/线上验证都通过。省略号改为仅在上下黑色区域出现（选取 .seg:not(.red)，红色区不会出现。省略号改为一组 6 个点（flex 排列），每个点随机大小 6～7.2px（默认值 6px 增幅不超 20%）。

## DGWEB_V0.18  （2026-08-05 21:30）

1. 修复活动关卡 EX/S/MO/S 关卡编号缺失：视频标题分支中 EX/MO/S 关卡（如 AS-EX-1、TA-MO-1、FM-S-1）无法从标题解析出编号，全程提取按 SEE"普通编号"处理。已修复 _build_modes.py 的 CODE_RE、源码截止段（含-1 支持），normalize_code 统一 AS-EX-1 式规范，EX/S/MO/S 分支对齐。
2. stage.html 的 matchCode / normCode 同步更新（normalizeCode），匹配前 toUpperCase 兼容 cv-ex-4 例外。
3. modes.json 重新聚合：event-stages 401/ESW-705 关（EX 260 / MO 8 / S 38），ESW 的 S 9 坐，其他模块 segment 的内容保持；阶段排序统一「普通→EX→S→MO」。
4. 关卡代码统一为「普通→EX→S→MO」排序（含关卡代码排序 AS-1~9、AS-EX-1~8、AS-S-1~4、AS-MO-1，_build_modes.py 的 stage_sort_key 实现）。
5. 新增 9 个覆盖场景测试（TD-EX-1 等），live 验证 714 个 chip 全部匹配。
6. 全链路回归验证：本地 714 关 chip、线上 EX/MO/S 关卡代码与视频对应关系全部匹配。
7. 修复导航<nav> 打开与闭合标签丢失导致的页面布局错乱（fixed top-0 层级、max-w-[1400px] 内容居中）
8. 关卡视频方案改版：theme-videos.html 改为按视频实际标题（含模式/地图）逐条匹配关卡，替代原来的整体对应方案；支持对主模式/活动/情景逐帧分析；模板页全部重写 URL。
9. 视频方案扩至全模式：所有模式关卡支持 — 关卡/关卡/BGM 全会话标签，活动/模式变化；13 个模式页 + 主题视频页联合行走。
10. 关卡视频方案扩至其他活动：其他活动模式下（纷争演绎、其它）全文并入 other-events，事件 2023 已复活且专区布局不复返；活动页专门区分 3 种专区类型；活动事件 2023 组 8 个；activity-in 逻辑修复（inv 切换老 bug）。
11. 「干员泛用榜」改名为「核心使用率」：ranking.html 的标题/H1/列表标题/动态标题（嵌套「使用率」）。全站 20 页导航与 footer 同步更新文案。
12. 全站移动端适配（手机/平板）：新增番茄盘移动菜单，<768px 显示汉堡按钮展开并排下拉菜单；320px 视口无横向滚动；程序化嵌入所有 20 个页面（源码锚点对齐、媒体查询注入最后 <style>、.nav-toggle 兼容 Tailwind md:hidden）；ranking 表格响应式压缩（320px 无溢出）；aliases 头像图 120px；detail/stage 表单响应式；修复 index 模式化移动端按钮体验。
13. 首页统计数字即时显示与动效优化：
    - 修复每次打开时统计数字按旧值滚动、需滚动才更新为最新：IntersectionObserver 在统计数据返回前触发动画的问题，新增 statsReady/heroRevealed 标记，数据就绪后再播（本地+线上验证）。
    - 动画期间统计框宽度固定（预测量最终文本宽度，数字在框内居中跳动）；高度同样测量后固定，nowrap 防换行（实测 169x58/100x58/202x58 恒定，手机无横溢）。
    - 统计标签补充中文：FOLLOWERS 前加「关注」、VIDEOS 前加「视频」、LIKES 前加「点赞」（.cn 类重置字母间距）。
    - 解除 LIKES 单元格 hidden sm:block 限制，手机端也显示点赞数据。
    - 修复老内核（搜狗等）不支持 AbortSignal.timeout 导致数据为 0：新增 fetchWithTimeout 逐级兼容（AbortSignal.timeout → AbortController+setTimeout → 无超时），并缓存 stats 回退。
14. 白昼/暗夜主题：
    - 全站 20 页注入右下角圆形切换按钮（月/日图标随主题，hover 旋转）。
    - html.light 规则覆盖 CSS 变量 + 全部 Tailwind 色值 utility（含透明度变体），并覆盖滚动条/噪声/加载屏/cursor 等特殊元素。
    - 切换动画：点击处同心圆扩散（theme-overlay + themeWipe keyframes，550ms 兜底 900ms）。
    - localStorage 记忆主题（dgweb_theme）+ head 防闪烁脚本。
    - 图标语义修正：按钮显示切换目标（暗夜显太阳、白昼显月亮）。
    - 悬停 title 同步联动（暗夜显示「白昼模式」、白昼显示「黑夜模式」）。

15. 新增干员别称：伊内丝→ins、星熊→熊大、斩业星熊→熊二、予愿安洁莉娜→茅圆；同步修改 aliases.html 的 ALIAS_OPS 与 ranking.html 的视频标题归并映射 ALIAS，本地/线上搜索 4 项均正常命中。

16. 彩蛋：一分钟内切换夜晚/白晐主题 4 次（全站 20 页任意页面）跳转 easter-egg.html。彩蛋页：上黑/中深红（#7a0c0c）/下黑三段布局，中红区中心浮现「是否确认关闭全舰防御系统？」，黑色区域每秒随机出现白色省略号；文字出现 4 秒后点击显示「是/否」，选择后屏幕变黑——选是显示「做得好，预言家。」，选否显示「谢谢你，博士」，7 秒后自动返回首页。本地/线上全流程验证通过。（迭代）选项↓移至 62%处；省略号改为常驻不销毁（每秒累积）；触发时播放《Storyteller》（塞居唱片，《巴别塔OST》首曲，收集直接下载转 192k mp3 自托管 easter-storyteller.mp3，autoplay 被拒时首次点击触发）。本地/线上验证都通过。省略号改为仅在上下黑色区域出现（选取 .seg:not(.red)，红色区不会出现。省略号改为一组 6 个点（flex 排列），每个点随机大小 6～7.2px（默认值 6px 增幅不超 20%）。

## DGWEB_V0.19  （2026-08-05 21:30）

1. 修复活动关卡 EX/S/MO/S 关卡编号缺失：视频标题分支中 EX/MO/S 关卡（如 AS-EX-1、TA-MO-1、FM-S-1）无法从标题解析出编号，全程提取按 SEE"普通编号"处理。已修复 _build_modes.py 的 CODE_RE、源码截止段（含-1 支持），normalize_code 统一 AS-EX-1 式规范，EX/S/MO/S 分支对齐。
2. stage.html 的 matchCode / normCode 同步更新（normalizeCode），匹配前 toUpperCase 兼容 cv-ex-4 例外。
3. modes.json 重新聚合：event-stages 401/ESW-705 关（EX 260 / MO 8 / S 38），ESW 的 S 9 坐，其他模块 segment 的内容保持；阶段排序统一「普通→EX→S→MO」。
4. 关卡代码统一为「普通→EX→S→MO」排序（含关卡代码排序 AS-1~9、AS-EX-1~8、AS-S-1~4、AS-MO-1，_build_modes.py 的 stage_sort_key 实现）。
5. 新增 9 个覆盖场景测试（TD-EX-1 等），live 验证 714 个 chip 全部匹配。
6. 全链路回归验证：本地 714 关 chip、线上 EX/MO/S 关卡代码与视频对应关系全部匹配。
7. 修复导航<nav> 打开与闭合标签丢失导致的页面布局错乱（fixed top-0 层级、max-w-[1400px] 内容居中）
8. 关卡视频方案改版：theme-videos.html 改为按视频实际标题（含模式/地图）逐条匹配关卡，替代原来的整体对应方案；支持对主模式/活动/情景逐帧分析；模板页全部重写 URL。
9. 视频方案扩至全模式：所有模式关卡支持 — 关卡/关卡/BGM 全会话标签，活动/模式变化；13 个模式页 + 主题视频页联合行走。
10. 关卡视频方案扩至其他活动：其他活动模式下（纷争演绎、其它）全文并入 other-events，事件 2023 已复活且专区布局不复返；活动页专门区分 3 种专区类型；活动事件 2023 组 8 个；activity-in 逻辑修复（inv 切换老 bug）。
11. 「干员泛用榜」改名为「核心使用率」：ranking.html 的标题/H1/列表标题/动态标题（嵌套「使用率」）。全站 20 页导航与 footer 同步更新文案。
12. 全站移动端适配（手机/平板）：新增番茄盘移动菜单，<768px 显示汉堡按钮展开并排下拉菜单；320px 视口无横向滚动；程序化嵌入所有 20 个页面（源码锚点对齐、媒体查询注入最后 <style>、.nav-toggle 兼容 Tailwind md:hidden）；ranking 表格响应式压缩（320px 无溢出）；aliases 头像图 120px；detail/stage 表单响应式；修复 index 模式化移动端按钮体验。
13. 首页统计数字即时显示与动效优化：
    - 修复每次打开时统计数字按旧值滚动、需滚动才更新为最新：IntersectionObserver 在统计数据返回前触发动画的问题，新增 statsReady/heroRevealed 标记，数据就绪后再播（本地+线上验证）。
    - 动画期间统计框宽度固定（预测量最终文本宽度，数字在框内居中跳动）；高度同样测量后固定，nowrap 防换行（实测 169x58/100x58/202x58 恒定，手机无横溢）。
    - 统计标签补充中文：FOLLOWERS 前加「关注」、VIDEOS 前加「视频」、LIKES 前加「点赞」（.cn 类重置字母间距）。
    - 解除 LIKES 单元格 hidden sm:block 限制，手机端也显示点赞数据。
    - 修复老内核（搜狗等）不支持 AbortSignal.timeout 导致数据为 0：新增 fetchWithTimeout 逐级兼容（AbortSignal.timeout → AbortController+setTimeout → 无超时），并缓存 stats 回退。
14. 白昼/暗夜主题：
    - 全站 20 页注入右下角圆形切换按钮（月/日图标随主题，hover 旋转）。
    - html.light 规则覆盖 CSS 变量 + 全部 Tailwind 色值 utility（含透明度变体），并覆盖滚动条/噪声/加载屏/cursor 等特殊元素。
    - 切换动画：点击处同心圆扩散（theme-overlay + themeWipe keyframes，550ms 兜底 900ms）。
    - localStorage 记忆主题（dgweb_theme）+ head 防闪烁脚本。
    - 图标语义修正：按钮显示切换目标（暗夜显太阳、白昼显月亮）。
    - 悬停 title 同步联动（暗夜显示「白昼模式」、白昼显示「黑夜模式」）。

15. 新增干员别称：伊内丝→ins、星熊→熊大、斩业星熊→熊二、予愿安洁莉娜→茅圆；同步修改 aliases.html 的 ALIAS_OPS 与 ranking.html 的视频标题归并映射 ALIAS，本地/线上搜索 4 项均正常命中。

16. 彩蛋：一分钟内切换夜晚/白晐主题 4 次（全站 20 页任意页面）跳转 easter-egg.html。彩蛋页：上黑/中深红（#7a0c0c）/下黑三段布局，中红区中心浮现「是否确认关闭全舰防御系统？」，黑色区域每秒随机出现白色省略号；文字出现 4 秒后点击显示「是/否」，选择后屏幕变黑——选是显示「做得好，预言家。」，选否显示「谢谢你，博士」，7 秒后自动返回首页。本地/线上全流程验证通过。（迭代）选项↓移至 62%处；省略号改为常驻不销毁（每秒累积）；触发时播放《Storyteller》（塞居唱片，《巴别塔OST》首曲，收集直接下载转 192k mp3 自托管 easter-storyteller.mp3，autoplay 被拒时首次点击触发）。本地/线上验证都通过。省略号改为仅在上下黑色区域出现（选取 .seg:not(.red)，红色区不会出现。省略号改为一组 6 个点（flex 排列），每个点随机大小 6～7.2px（默认值 6px 增幅不超 20%）。

## DGWEB_V0.20  （2026-08-06 12:32）

- 手动触发验证一次吧
- [attempt 1] HTTP 412 / [attempt 2] HTTP 412
- 行，把项目文件夹命名V0.01，之后每次改动都复制一个副本命名都+0.01，同时每个版本都生成一个说明文件标注做了哪些改动
- 每次修改项目先创建新版本直到我说没问题停止新版本定格，确认实现后下次增加功能或修改再复制一次项目修改
- Continue if you have next steps, or stop and ask for clarification if you are unsure how to proceed.
- 定格版本时根据opencode记录生成版本说明，同时把目前的交流记录整理一下写一份AI衔接进度文档，方便其他AI理解项目

## DGWEB_V0.21  （2026-08-06 13:39）

- 手动触发验证一次吧
- [attempt 1] HTTP 412 / [attempt 2] HTTP 412
- 行，把项目文件夹命名V0.01，之后每次改动都复制一个副本命名都+0.01，同时每个版本都生成一个说明文件标注做了哪些改动
- 每次修改项目先创建新版本直到我说没问题停止新版本定格，确认实现后下次增加功能或修改再复制一次项目修改
- Continue if you have next steps, or stop and ask for clarification if you are unsure how to proceed.
- 定格版本时根据opencode记录生成版本说明，同时把目前的交流记录整理一下写一份AI衔接进度文档，方便其他AI理解项目

