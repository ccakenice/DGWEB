# DGWEB — 版本变更历史（已定格版本，由管理器自动汇总）
最后更新：2026-08-12 19:37

> 说明：每次「定格版本」后，点击「理解AI」会自动重新汇总全部已定格版本的改动。
> AI 阅读时请按版本号从旧到新浏览，关注最新定格改动的功能与实现。

## DGWEB_V0.02  （2026-08-01 22:41）

- 手动触发验证一次吧
- [attempt 1] HTTP 412 / [attempt 2] HTTP 412
- 行，把项目文件夹命名V0.01，之后每次改动都复制一个副本命名都+0.01，同时每个版本都生成一个说明文件标注做了哪些改动
- 每次修改项目先创建新版本直到我说没问题停止新版本定格，确认实现后下次增加功能或修改再复制一次项目修改
- Continue if you have next steps, or stop and ask for clarification if you are unsure how to proceed.
- 定格版本时根据opencode记录生成版本说明，同时把目前的交流记录整理一下写一份AI衔接进度文档，方便其他AI理解项目

## DGWEB_V0.06  （2026-08-02 01:15）

- 数据源升级：视频列表从 arc/search 接口（硬限制最新 180 条）改为 search/type 搜索接口
  （关键词=UP主名"低配挂机研讨会"，按发布翻页 12 页，过滤 mid），成功抓取全量 512 个视频
  （archive_count 528 含不可见的已删/审核中稿件）。
- 新增 SESSDATA 登录 Cookie 支持（本地使用，不上传），buvid3/4 + SESSDATA 即可访问搜索接口，
  显著降低 B站 412 风控概率。
- 412 快速失败策略：每页最多重试 3 次（等待 5s/10s/20s），遇 412 立即停止；
  stats 统计优先更新，抓取失败时复用本地缓存。
- 统计第三项从 PLAYLISTS（合集数，接口 404 无法获取）改为 LIKES（获赞数，card 接口 like_num）。
- index.html 统计元素改为 statLikes / LIKES，applyStats 映射 stats.likes。
- HTML 标签清理：新增 clean_html() 剥离搜索接口返回的 <em class="keyword"> 高亮标签（363 条受影响），
  避免前端显示异常。
- 缓存兜底：新增 load_cache()，视频或统计获取失败时自动复用本地 latest.json 上次成功数据。
- 数据托管升级：latest.json 同步推送 bili-sync（CDN）和 DGWEB 仓库（本站托管），
  前端 DATA_URLS 多源 fallback：本站 /latest.json → jsDelivr CDN → GitHub raw。
- 数据缩量防护：抓取结果与旧缓存按 link 合并去重取并集，避免搜索接口分页抖动导致数据缩量。
- 本次实测数据：512 视频（2023-09 至 2026-08），fans≈191900 / videos=528 / likes≈1178600。

## DGWEB_V0.08  （2026-08-03 11:19）

- 手动触发验证一次吧
- [attempt 1] HTTP 412 / [attempt 2] HTTP 412
- 行，把项目文件夹命名V0.01，之后每次改动都复制一个副本命名都+0.01，同时每个版本都生成一个说明文件标注做了哪些改动
- 每次修改项目先创建新版本直到我说没问题停止新版本定格，确认实现后下次增加功能或修改再复制一次项目修改
- Continue if you have next steps, or stop and ask for clarification if you are unsure how to proceed.
- 定格版本时根据opencode记录生成版本说明，同时把目前的交流记录整理一下写一份AI衔接进度文档，方便其他AI理解项目

## DGWEB_V0.09  （2026-08-03 20:48）

- 手动触发验证一次吧
- [attempt 1] HTTP 412 / [attempt 2] HTTP 412
- 行，把项目文件夹命名V0.01，之后每次改动都复制一个副本命名都+0.01，同时每个版本都生成一个说明文件标注做了哪些改动
- 每次修改项目先创建新版本直到我说没问题停止新版本定格，确认实现后下次增加功能或修改再复制一次项目修改
- Continue if you have next steps, or stop and ask for clarification if you are unsure how to proceed.
- 定格版本时根据opencode记录生成版本说明，同时把目前的交流记录整理一下写一份AI衔接进度文档，方便其他AI理解项目

## DGWEB_V0.10  （2026-08-03 23:34）

- 手动触发验证一次吧
- [attempt 1] HTTP 412 / [attempt 2] HTTP 412
- 行，把项目文件夹命名V0.01，之后每次改动都复制一个副本命名都+0.01，同时每个版本都生成一个说明文件标注做了哪些改动
- 每次修改项目先创建新版本直到我说没问题停止新版本定格，确认实现后下次增加功能或修改再复制一次项目修改
- Continue if you have next steps, or stop and ask for clarification if you are unsure how to proceed.
- 定格版本时根据opencode记录生成版本说明，同时把目前的交流记录整理一下写一份AI衔接进度文档，方便其他AI理解项目

## DGWEB_V0.11  （2026-08-04 14:44）

- 手动触发验证一次吧
- [attempt 1] HTTP 412 / [attempt 2] HTTP 412
- 行，把项目文件夹命名V0.01，之后每次改动都复制一个副本命名都+0.01，同时每个版本都生成一个说明文件标注做了哪些改动
- 每次修改项目先创建新版本直到我说没问题停止新版本定格，确认实现后下次增加功能或修改再复制一次项目修改
- Continue if you have next steps, or stop and ask for clarification if you are unsure how to proceed.
- 定格版本时根据opencode记录生成版本说明，同时把目前的交流记录整理一下写一份AI衔接进度文档，方便其他AI理解项目

## DGWEB_V0.12  （2026-08-05 13:52）

- 手动触发验证一次吧
- [attempt 1] HTTP 412 / [attempt 2] HTTP 412
- 行，把项目文件夹命名V0.01，之后每次改动都复制一个副本命名都+0.01，同时每个版本都生成一个说明文件标注做了哪些改动
- 每次修改项目先创建新版本直到我说没问题停止新版本定格，确认实现后下次增加功能或修改再复制一次项目修改
- Continue if you have next steps, or stop and ask for clarification if you are unsure how to proceed.
- 定格版本时根据opencode记录生成版本说明，同时把目前的交流记录整理一下写一份AI衔接进度文档，方便其他AI理解项目

## DGWEB_V0.14  （2026-08-05 14:08）

- 手动触发验证一次吧
- [attempt 1] HTTP 412 / [attempt 2] HTTP 412
- 行，把项目文件夹命名V0.01，之后每次改动都复制一个副本命名都+0.01，同时每个版本都生成一个说明文件标注做了哪些改动
- 每次修改项目先创建新版本直到我说没问题停止新版本定格，确认实现后下次增加功能或修改再复制一次项目修改
- Continue if you have next steps, or stop and ask for clarification if you are unsure how to proceed.
- 定格版本时根据opencode记录生成版本说明，同时把目前的交流记录整理一下写一份AI衔接进度文档，方便其他AI理解项目

## DGWEB_V0.15  （2026-08-05 16:59）

- 手动触发验证一次吧
- [attempt 1] HTTP 412 / [attempt 2] HTTP 412
- 行，把项目文件夹命名V0.01，之后每次改动都复制一个副本命名都+0.01，同时每个版本都生成一个说明文件标注做了哪些改动
- 每次修改项目先创建新版本直到我说没问题停止新版本定格，确认实现后下次增加功能或修改再复制一次项目修改
- Continue if you have next steps, or stop and ask for clarification if you are unsure how to proceed.
- 定格版本时根据opencode记录生成版本说明，同时把目前的交流记录整理一下写一份AI衔接进度文档，方便其他AI理解项目

## DGWEB_V0.16  （2026-08-05 16:59）

- 手动触发验证一次吧
- [attempt 1] HTTP 412 / [attempt 2] HTTP 412
- 行，把项目文件夹命名V0.01，之后每次改动都复制一个副本命名都+0.01，同时每个版本都生成一个说明文件标注做了哪些改动
- 每次修改项目先创建新版本直到我说没问题停止新版本定格，确认实现后下次增加功能或修改再复制一次项目修改
- Continue if you have next steps, or stop and ask for clarification if you are unsure how to proceed.
- 定格版本时根据opencode记录生成版本说明，同时把目前的交流记录整理一下写一份AI衔接进度文档，方便其他AI理解项目

## DGWEB_V0.17  （2026-08-05 21:30）

1. 修复活动关卡 EX/S/MO/S 关卡编号缺失：视频标题分支中 EX/MO/S 关卡（如 AS-EX-1、TA-MO-1、FM-S-1）无法从标题解析出编号，全程提取按 SEE"普通编号"处理。已修复 _build_modes.py 的 CODE_RE、源码截止段（含-1 支持），normalize_code 统一 AS-EX-1 式规范，EX/S/MO/S 分支对齐。
2. stage.html 的 matchCode / normCode 同步更新（normalizeCode），匹配前 toUpperCase 兼容 cv-ex-4 例外。
3. modes.json 重新聚合：event-stages 401/ESW-705 关（EX 260 / MO 8 / S 38），ESW 的 S 9 坐，其他模块 segment 的内容保持；阶段排序统一「普通→EX→S→MO」。
4. 关卡代码统一为「普通→EX→S→MO」排序（含关卡代码排序 AS-1~9、AS-EX-1~8、AS-S-1~4、AS-MO-1，_build_modes.py 的 stage_sort_key 实现）。
5. 新增 9 个覆盖场景测试（TD-EX-1 等），live 验证 714 个 chip 全部匹配。
6. 全链路回归验证：本地 714 关 chip、线上 EX/MO/S 关卡代码与视频对应关系全部匹配。
7. 修复导航<nav> 打开与闭合标签丢失导致的页面布局错乱（fixed top-0 层级、max-w-[1400px] 内容居中）
8. 关卡视频方案改版：theme-videos.html 改为按视频实际标题（含模式/地图）逐条匹配关卡，替代原来的整体对应方案；支持对主模式/活动/情景逐帧分析；模板页全部重写 URL。
9. 视频方案扩至全模式：所有模式关卡支持 — 关卡/关卡/BGM 全会话标签，活动/模式变化；13 个模式页 + 主题视频页联合行走。
10. 关卡视频方案扩至其他活动：其他活动模式下（纷争演绎、其它）全文并入 other-events，事件 2023 已复活且专区布局不复返；活动页专门区分 3 种专区类型；活动事件 2023 组 8 个；activity-in 逻辑修复（inv 切换老 bug）。
11. 「干员泛用榜」改名为「核心使用率」：ranking.html 的标题/H1/列表标题/动态标题（嵌套「使用率」）。全站 20 页导航与 footer 同步更新文案。
12. 全站移动端适配（手机/平板）：新增番茄盘移动菜单，<768px 显示汉堡按钮展开并排下拉菜单；320px 视口无横向滚动；程序化嵌入所有 20 个页面（源码锚点对齐、媒体查询注入最后 <style>、.nav-toggle 兼容 Tailwind md:hidden）；ranking 表格响应式压缩（320px 无溢出）；aliases 头像图 120px；detail/stage 表单响应式；修复 index 模式化移动端按钮体验。
13. 首页统计数字即时显示与动效优化：
    - 修复每次打开时统计数字按旧值滚动、需滚动才更新为最新：IntersectionObserver 在统计数据返回前触发动画的问题，新增 statsReady/heroRevealed 标记，数据就绪后再播（本地+线上验证）。
    - 动画期间统计框宽度固定（预测量最终文本宽度，数字在框内居中跳动）；高度同样测量后固定，nowrap 防换行（实测 169x58/100x58/202x58 恒定，手机无横溢）。
    - 统计标签补充中文：FOLLOWERS 前加「关注」、VIDEOS 前加「视频」、LIKES 前加「点赞」（.cn 类重置字母间距）。
    - 解除 LIKES 单元格 hidden sm:block 限制，手机端也显示点赞数据。
    - 修复老内核（搜狗等）不支持 AbortSignal.timeout 导致数据为 0：新增 fetchWithTimeout 逐级兼容（AbortSignal.timeout → AbortController+setTimeout → 无超时），并缓存 stats 回退。
14. 白昼/暗夜主题：
    - 全站 20 页注入右下角圆形切换按钮（月/日图标随主题，hover 旋转）。
    - html.light 规则覆盖 CSS 变量 + 全部 Tailwind 色值 utility（含透明度变体），并覆盖滚动条/噪声/加载屏/cursor 等特殊元素。
    - 切换动画：点击处同心圆扩散（theme-overlay + themeWipe keyframes，550ms 兜底 900ms）。
    - localStorage 记忆主题（dgweb_theme）+ head 防闪烁脚本。
    - 图标语义修正：按钮显示切换目标（暗夜显太阳、白昼显月亮）。
    - 悬停 title 同步联动（暗夜显示「白昼模式」、白昼显示「黑夜模式」）。

15. 新增干员别称：伊内丝→ins、星熊→熊大、斩业星熊→熊二、予愿安洁莉娜→茅圆；同步修改 aliases.html 的 ALIAS_OPS 与 ranking.html 的视频标题归并映射 ALIAS，本地/线上搜索 4 项均正常命中。

16. 彩蛋：一分钟内切换夜晚/白晐主题 4 次（全站 20 页任意页面）跳转 easter-egg.html。彩蛋页：上黑/中深红（#7a0c0c）/下黑三段布局，中红区中心浮现「是否确认关闭全舰防御系统？」，黑色区域每秒随机出现白色省略号；文字出现 4 秒后点击显示「是/否」，选择后屏幕变黑——选是显示「做得好，预言家。」，选否显示「谢谢你，博士」，7 秒后自动返回首页。本地/线上全流程验证通过。（迭代）选项↓移至 62%处；省略号改为常驻不销毁（每秒累积）；触发时播放《Storyteller》（塞居唱片，《巴别塔OST》首曲，收集直接下载转 192k mp3 自托管 easter-storyteller.mp3，autoplay 被拒时首次点击触发）。本地/线上验证都通过。省略号改为仅在上下黑色区域出现（选取 .seg:not(.red)，红色区不会出现。省略号改为一组 6 个点（flex 排列），每个点随机大小 6～7.2px（默认值 6px 增幅不超 20%）。

## DGWEB_V0.18  （2026-08-05 21:30）

1. 修复活动关卡 EX/S/MO/S 关卡编号缺失：视频标题分支中 EX/MO/S 关卡（如 AS-EX-1、TA-MO-1、FM-S-1）无法从标题解析出编号，全程提取按 SEE"普通编号"处理。已修复 _build_modes.py 的 CODE_RE、源码截止段（含-1 支持），normalize_code 统一 AS-EX-1 式规范，EX/S/MO/S 分支对齐。
2. stage.html 的 matchCode / normCode 同步更新（normalizeCode），匹配前 toUpperCase 兼容 cv-ex-4 例外。
3. modes.json 重新聚合：event-stages 401/ESW-705 关（EX 260 / MO 8 / S 38），ESW 的 S 9 坐，其他模块 segment 的内容保持；阶段排序统一「普通→EX→S→MO」。
4. 关卡代码统一为「普通→EX→S→MO」排序（含关卡代码排序 AS-1~9、AS-EX-1~8、AS-S-1~4、AS-MO-1，_build_modes.py 的 stage_sort_key 实现）。
5. 新增 9 个覆盖场景测试（TD-EX-1 等），live 验证 714 个 chip 全部匹配。
6. 全链路回归验证：本地 714 关 chip、线上 EX/MO/S 关卡代码与视频对应关系全部匹配。
7. 修复导航<nav> 打开与闭合标签丢失导致的页面布局错乱（fixed top-0 层级、max-w-[1400px] 内容居中）
8. 关卡视频方案改版：theme-videos.html 改为按视频实际标题（含模式/地图）逐条匹配关卡，替代原来的整体对应方案；支持对主模式/活动/情景逐帧分析；模板页全部重写 URL。
9. 视频方案扩至全模式：所有模式关卡支持 — 关卡/关卡/BGM 全会话标签，活动/模式变化；13 个模式页 + 主题视频页联合行走。
10. 关卡视频方案扩至其他活动：其他活动模式下（纷争演绎、其它）全文并入 other-events，事件 2023 已复活且专区布局不复返；活动页专门区分 3 种专区类型；活动事件 2023 组 8 个；activity-in 逻辑修复（inv 切换老 bug）。
11. 「干员泛用榜」改名为「核心使用率」：ranking.html 的标题/H1/列表标题/动态标题（嵌套「使用率」）。全站 20 页导航与 footer 同步更新文案。
12. 全站移动端适配（手机/平板）：新增番茄盘移动菜单，<768px 显示汉堡按钮展开并排下拉菜单；320px 视口无横向滚动；程序化嵌入所有 20 个页面（源码锚点对齐、媒体查询注入最后 <style>、.nav-toggle 兼容 Tailwind md:hidden）；ranking 表格响应式压缩（320px 无溢出）；aliases 头像图 120px；detail/stage 表单响应式；修复 index 模式化移动端按钮体验。
13. 首页统计数字即时显示与动效优化：
    - 修复每次打开时统计数字按旧值滚动、需滚动才更新为最新：IntersectionObserver 在统计数据返回前触发动画的问题，新增 statsReady/heroRevealed 标记，数据就绪后再播（本地+线上验证）。
    - 动画期间统计框宽度固定（预测量最终文本宽度，数字在框内居中跳动）；高度同样测量后固定，nowrap 防换行（实测 169x58/100x58/202x58 恒定，手机无横溢）。
    - 统计标签补充中文：FOLLOWERS 前加「关注」、VIDEOS 前加「视频」、LIKES 前加「点赞」（.cn 类重置字母间距）。
    - 解除 LIKES 单元格 hidden sm:block 限制，手机端也显示点赞数据。
    - 修复老内核（搜狗等）不支持 AbortSignal.timeout 导致数据为 0：新增 fetchWithTimeout 逐级兼容（AbortSignal.timeout → AbortController+setTimeout → 无超时），并缓存 stats 回退。
14. 白昼/暗夜主题：
    - 全站 20 页注入右下角圆形切换按钮（月/日图标随主题，hover 旋转）。
    - html.light 规则覆盖 CSS 变量 + 全部 Tailwind 色值 utility（含透明度变体），并覆盖滚动条/噪声/加载屏/cursor 等特殊元素。
    - 切换动画：点击处同心圆扩散（theme-overlay + themeWipe keyframes，550ms 兜底 900ms）。
    - localStorage 记忆主题（dgweb_theme）+ head 防闪烁脚本。
    - 图标语义修正：按钮显示切换目标（暗夜显太阳、白昼显月亮）。
    - 悬停 title 同步联动（暗夜显示「白昼模式」、白昼显示「黑夜模式」）。

15. 新增干员别称：伊内丝→ins、星熊→熊大、斩业星熊→熊二、予愿安洁莉娜→茅圆；同步修改 aliases.html 的 ALIAS_OPS 与 ranking.html 的视频标题归并映射 ALIAS，本地/线上搜索 4 项均正常命中。

16. 彩蛋：一分钟内切换夜晚/白晐主题 4 次（全站 20 页任意页面）跳转 easter-egg.html。彩蛋页：上黑/中深红（#7a0c0c）/下黑三段布局，中红区中心浮现「是否确认关闭全舰防御系统？」，黑色区域每秒随机出现白色省略号；文字出现 4 秒后点击显示「是/否」，选择后屏幕变黑——选是显示「做得好，预言家。」，选否显示「谢谢你，博士」，7 秒后自动返回首页。本地/线上全流程验证通过。（迭代）选项↓移至 62%处；省略号改为常驻不销毁（每秒累积）；触发时播放《Storyteller》（塞居唱片，《巴别塔OST》首曲，收集直接下载转 192k mp3 自托管 easter-storyteller.mp3，autoplay 被拒时首次点击触发）。本地/线上验证都通过。省略号改为仅在上下黑色区域出现（选取 .seg:not(.red)，红色区不会出现。省略号改为一组 6 个点（flex 排列），每个点随机大小 6～7.2px（默认值 6px 增幅不超 20%）。

## DGWEB_V0.19  （2026-08-05 21:30）

1. 修复活动关卡 EX/S/MO/S 关卡编号缺失：视频标题分支中 EX/MO/S 关卡（如 AS-EX-1、TA-MO-1、FM-S-1）无法从标题解析出编号，全程提取按 SEE"普通编号"处理。已修复 _build_modes.py 的 CODE_RE、源码截止段（含-1 支持），normalize_code 统一 AS-EX-1 式规范，EX/S/MO/S 分支对齐。
2. stage.html 的 matchCode / normCode 同步更新（normalizeCode），匹配前 toUpperCase 兼容 cv-ex-4 例外。
3. modes.json 重新聚合：event-stages 401/ESW-705 关（EX 260 / MO 8 / S 38），ESW 的 S 9 坐，其他模块 segment 的内容保持；阶段排序统一「普通→EX→S→MO」。
4. 关卡代码统一为「普通→EX→S→MO」排序（含关卡代码排序 AS-1~9、AS-EX-1~8、AS-S-1~4、AS-MO-1，_build_modes.py 的 stage_sort_key 实现）。
5. 新增 9 个覆盖场景测试（TD-EX-1 等），live 验证 714 个 chip 全部匹配。
6. 全链路回归验证：本地 714 关 chip、线上 EX/MO/S 关卡代码与视频对应关系全部匹配。
7. 修复导航<nav> 打开与闭合标签丢失导致的页面布局错乱（fixed top-0 层级、max-w-[1400px] 内容居中）
8. 关卡视频方案改版：theme-videos.html 改为按视频实际标题（含模式/地图）逐条匹配关卡，替代原来的整体对应方案；支持对主模式/活动/情景逐帧分析；模板页全部重写 URL。
9. 视频方案扩至全模式：所有模式关卡支持 — 关卡/关卡/BGM 全会话标签，活动/模式变化；13 个模式页 + 主题视频页联合行走。
10. 关卡视频方案扩至其他活动：其他活动模式下（纷争演绎、其它）全文并入 other-events，事件 2023 已复活且专区布局不复返；活动页专门区分 3 种专区类型；活动事件 2023 组 8 个；activity-in 逻辑修复（inv 切换老 bug）。
11. 「干员泛用榜」改名为「核心使用率」：ranking.html 的标题/H1/列表标题/动态标题（嵌套「使用率」）。全站 20 页导航与 footer 同步更新文案。
12. 全站移动端适配（手机/平板）：新增番茄盘移动菜单，<768px 显示汉堡按钮展开并排下拉菜单；320px 视口无横向滚动；程序化嵌入所有 20 个页面（源码锚点对齐、媒体查询注入最后 <style>、.nav-toggle 兼容 Tailwind md:hidden）；ranking 表格响应式压缩（320px 无溢出）；aliases 头像图 120px；detail/stage 表单响应式；修复 index 模式化移动端按钮体验。
13. 首页统计数字即时显示与动效优化：
    - 修复每次打开时统计数字按旧值滚动、需滚动才更新为最新：IntersectionObserver 在统计数据返回前触发动画的问题，新增 statsReady/heroRevealed 标记，数据就绪后再播（本地+线上验证）。
    - 动画期间统计框宽度固定（预测量最终文本宽度，数字在框内居中跳动）；高度同样测量后固定，nowrap 防换行（实测 169x58/100x58/202x58 恒定，手机无横溢）。
    - 统计标签补充中文：FOLLOWERS 前加「关注」、VIDEOS 前加「视频」、LIKES 前加「点赞」（.cn 类重置字母间距）。
    - 解除 LIKES 单元格 hidden sm:block 限制，手机端也显示点赞数据。
    - 修复老内核（搜狗等）不支持 AbortSignal.timeout 导致数据为 0：新增 fetchWithTimeout 逐级兼容（AbortSignal.timeout → AbortController+setTimeout → 无超时），并缓存 stats 回退。
14. 白昼/暗夜主题：
    - 全站 20 页注入右下角圆形切换按钮（月/日图标随主题，hover 旋转）。
    - html.light 规则覆盖 CSS 变量 + 全部 Tailwind 色值 utility（含透明度变体），并覆盖滚动条/噪声/加载屏/cursor 等特殊元素。
    - 切换动画：点击处同心圆扩散（theme-overlay + themeWipe keyframes，550ms 兜底 900ms）。
    - localStorage 记忆主题（dgweb_theme）+ head 防闪烁脚本。
    - 图标语义修正：按钮显示切换目标（暗夜显太阳、白昼显月亮）。
    - 悬停 title 同步联动（暗夜显示「白昼模式」、白昼显示「黑夜模式」）。

15. 新增干员别称：伊内丝→ins、星熊→熊大、斩业星熊→熊二、予愿安洁莉娜→茅圆；同步修改 aliases.html 的 ALIAS_OPS 与 ranking.html 的视频标题归并映射 ALIAS，本地/线上搜索 4 项均正常命中。

16. 彩蛋：一分钟内切换夜晚/白晐主题 4 次（全站 20 页任意页面）跳转 easter-egg.html。彩蛋页：上黑/中深红（#7a0c0c）/下黑三段布局，中红区中心浮现「是否确认关闭全舰防御系统？」，黑色区域每秒随机出现白色省略号；文字出现 4 秒后点击显示「是/否」，选择后屏幕变黑——选是显示「做得好，预言家。」，选否显示「谢谢你，博士」，7 秒后自动返回首页。本地/线上全流程验证通过。（迭代）选项↓移至 62%处；省略号改为常驻不销毁（每秒累积）；触发时播放《Storyteller》（塞居唱片，《巴别塔OST》首曲，收集直接下载转 192k mp3 自托管 easter-storyteller.mp3，autoplay 被拒时首次点击触发）。本地/线上验证都通过。省略号改为仅在上下黑色区域出现（选取 .seg:not(.red)，红色区不会出现。省略号改为一组 6 个点（flex 排列），每个点随机大小 6～7.2px（默认值 6px 增幅不超 20%）。

## DGWEB_V0.20  （2026-08-06 12:32）

- 手动触发验证一次吧
- [attempt 1] HTTP 412 / [attempt 2] HTTP 412
- 行，把项目文件夹命名V0.01，之后每次改动都复制一个副本命名都+0.01，同时每个版本都生成一个说明文件标注做了哪些改动
- 每次修改项目先创建新版本直到我说没问题停止新版本定格，确认实现后下次增加功能或修改再复制一次项目修改
- Continue if you have next steps, or stop and ask for clarification if you are unsure how to proceed.
- 定格版本时根据opencode记录生成版本说明，同时把目前的交流记录整理一下写一份AI衔接进度文档，方便其他AI理解项目

## DGWEB_V0.21  （2026-08-06 13:39）

- 手动触发验证一次吧
- [attempt 1] HTTP 412 / [attempt 2] HTTP 412
- 行，把项目文件夹命名V0.01，之后每次改动都复制一个副本命名都+0.01，同时每个版本都生成一个说明文件标注做了哪些改动
- 每次修改项目先创建新版本直到我说没问题停止新版本定格，确认实现后下次增加功能或修改再复制一次项目修改
- Continue if you have next steps, or stop and ask for clarification if you are unsure how to proceed.
- 定格版本时根据opencode记录生成版本说明，同时把目前的交流记录整理一下写一份AI衔接进度文档，方便其他AI理解项目

## DGWEB_V0.22  （2026-08-07 08:12）

- 手动触发验证一次吧
- [attempt 1] HTTP 412 / [attempt 2] HTTP 412
- 行，把项目文件夹命名V0.01，之后每次改动都复制一个副本命名都+0.01，同时每个版本都生成一个说明文件标注做了哪些改动
- 每次修改项目先创建新版本直到我说没问题停止新版本定格，确认实现后下次增加功能或修改再复制一次项目修改
- Continue if you have next steps, or stop and ask for clarification if you are unsure how to proceed.
- 定格版本时根据opencode记录生成版本说明，同时把目前的交流记录整理一下写一份AI衔接进度文档，方便其他AI理解项目

## DGWEB_V0.23  （2026-08-07 09:53）

- 手动触发验证一次吧
- [attempt 1] HTTP 412 / [attempt 2] HTTP 412
- 行，把项目文件夹命名V0.01，之后每次改动都复制一个副本命名都+0.01，同时每个版本都生成一个说明文件标注做了哪些改动
- 每次修改项目先创建新版本直到我说没问题停止新版本定格，确认实现后下次增加功能或修改再复制一次项目修改
- Continue if you have next steps, or stop and ask for clarification if you are unsure how to proceed.
- 定格版本时根据opencode记录生成版本说明，同时把目前的交流记录整理一下写一份AI衔接进度文档，方便其他AI理解项目

## DGWEB_V0.24  （2026-08-07 14:07）

- 手动触发验证一次吧
- [attempt 1] HTTP 412 / [attempt 2] HTTP 412
- 行，把项目文件夹命名V0.01，之后每次改动都复制一个副本命名都+0.01，同时每个版本都生成一个说明文件标注做了哪些改动
- 每次修改项目先创建新版本直到我说没问题停止新版本定格，确认实现后下次增加功能或修改再复制一次项目修改
- Continue if you have next steps, or stop and ask for clarification if you are unsure how to proceed.
- 定格版本时根据opencode记录生成版本说明，同时把目前的交流记录整理一下写一份AI衔接进度文档，方便其他AI理解项目

## DGWEB_V0.25  （2026-08-08 01:20）

- PV 下载期无灰层：blob 完成瞬间标记 stage 触发时机从「立即显示」改为 `<video>` `playing` 事件（画面渲染后才显示），并加 4s `pvStageTimer` 兜底（自动播放被拒时确保点亮）；iframe 兜底延迟 1200ms→5000ms
- 进度条消失回归修复：用户交互（滚动/点击）触发 pvRender 时无条件重置 #pvProgress（loading 移除+宽度清零）；改为 blob 分段下载中（window.__pvBlobLoading）不重置，进度条全程保持 loading 至 100%
- 白昼过度修复：白昼 .pv-stage::after 白色渐变遮罩（opacity:1）持续叠在画面上造成视频过曝；移除白昼白场覆写，恢复黑渐变遮罩 opacity .72，白场仅保留在 fade 过渡瞬间（露出 #f4f4f2 背景）
- 线上验证：白昼 body active 时 afterBg 为黑渐变、画面中央像素 RGB(87,86,87) 无过曝；blob 100% 后 stage 点亮、视频播暂停为空、无 error
- [attempt 1] HTTP 412 / [attempt 2] HTTP 412
- 行，把项目文件夹命名V0.01，之后每次改动都复制一个副本命名都+0.01，同时每个版本都生成一个说明文件标注做了哪些改动
- 每次修改项目先创建新版本直到我说没问题停止新版本定格，确认实现后下次增加功能或修改再复制一次项目修改
- Continue if you have next steps, or stop and ask for clarification if you are unsure how to proceed.
- 定格版本时根据opencode记录生成版本说明，同时把目前的交流记录整理一下写一份AI衔接进度文档，方便其他AI理解项目


## V0.27（草稿）  2026-08-10
- api verification（API 验证）

## V0.28（草稿）  2026-08-11
- 核心使用率与别名干员头像修复：全部改用本地头像（141 张官方头像，纯 hash 文件名 a_[hash].png，375 处引用），两个页面无远程头像引用
- 社区工具修正：图米米→特米米；公招计算跳转改为 https://prts.wiki/w/公招计算（26 页面）
- PV 质量降级保护与 BILI_SESSDATA 更新（quality=80/1080P）
- 本版本由主目录全量对齐同步（175 个文件更新，清理 141 个旧编码头像文件名）

## DGWEB_V0.29  （2026-08-15 定格）
- V0.29 经用户确认于 2026-08-15 定格，复刻为 V0.30 继续开发
- 【2026-08-14 全量部署上线】map-sim.html 3D 地图模拟 / enemy.html 敌人图鉴 / data 全部索引与 36 个剿灭关卡本地 JSON / functions/spine/[[path]].js 同源代理 / js/spine 3.8 库 / images/map 官方地图贴图 / data/highland_colors.json 高台配色；单次提交推送 GitHub main，CF Pages 自动构建；补齐 camp_r_20（千嶂边城）、camp_r_23（燃烧街区）本地关卡 JSON
- 【敌人模型线上修复】Spine 资源改从 GitHub 公开仓库 serine-qing/MapSimulatorBackend public/ 经 jsDelivr/raw 直接加载；/spine 代理仅保留数据接口；线上验证默祷圣祠 401/401、0-1 11/11 敌人应用模型
- 【回退占位贴地 + 头像回退移除】Spine 加载失败时占位/头像不再悬浮：占位底部对齐画布底；按用户要求删除头像立绘回退，仅保留位置标记（圆标+阴影）
- 【脚底锚点改全程最低点】跑动/弹跳动画循环内持续取脚底全程最低点锁定（跑完 1.3 圈），GROUND_SINK 0.08→0.02→0.045（防压扁/压跑动空隙）
- 【身体后仰补偿（定位错误根因）】纸片模型倾斜使身体偏移到格子后方；modelLeanShift 前移 0.45*scl*sin(tilt) 对齐格心；ENEMY_MODEL_TILT 恢复 -0.5；验证脚贴参考环、身体居中
- 【模型尺寸与相机】MODEL_SCALE_MULT 恢复 1.0（官方尺寸），相机俯仰定 52°（45° 显矮、57.5° 官方角度太扁）
- 【罚站切待机动画】WAIT 检查点/移速 0 期间播 Idle，恢复移动切回 Move
- 【模型清晰度】Spine 离屏渲染画布 128→256（缩放公式 19.2→38.4、归一化 /256）；修复 256 行拷贝遗留 bug（每行只复制 512 字节致右半透明，改 1024 整行），19 敌人+3 道具全宽渲染；版本标记 v20260814a→g
- 【交互增强】空格键暂停/播放；全屏关卡按钮（#fsArea 容器，含工具栏/地图/卡片/进度条）；缩放锁定按钮（默认锁定）；地面参考诊断按钮
- 敌人详情页（V0.29 内含 enemy.html）：头像/名称/ID/官方描述/能力/基本信息/技能/天赋参数/抗性；PRTS 可读中文描述
- 叠图升级：官方地图低透明底图 + 透明度/缩放/X/Y 偏移滑块

## DGWEB_V0.30  （草稿）  2026-08-15
- 由 V0.29（定格）复刻创建
- 【官方地块固有色】新增 data/tile_colors.json（61 条，覆盖本地关卡 36 种地块）：官方 gamedata 无配色字段，颜色取自参考站 MapSimulator 源码硬编码固有色（ref_solid）+ 官方地图纹理图集 tiles1.png 单元格采样（atlas）+ 参考站默认规则（ref_default，官方无专色的 creep/mire/rope 等）
- 【地图模拟官方配色】map-sim.html 加载 tile_colors.json 覆盖 TILE_COLORS：高台 #c1c1c1、路面 #747474、禁行 #191919、地面 #555146、深水 #086e8d；红蓝门/绿门用官方描边色（#e03253/#359dde/#32d74b）保持可见；失败保留原深色兜底；版本标记 v20260815a，已部署上线试运行
- 【官方配色已回退】用户反馈纯色平涂太丑、无场景模型衬托；撤掉 TILE_COLOR_URLS 与 init 覆盖，恢复 V0.29 深色 TILE_COLORS；tile_colors.json 保留作数据参考不再应用；版本标记 v20260815a→b
- 【飞行敌人高度修复】飞行敌人原逐帧贴地、过高低台上下起伏；路线构建时按全程最高地形（检查点+线段 0.25 格采样，覆盖墙顶）计算固定飞行高度 routeFlyY=最高地形顶+0.25，飞行脚底恒定、阴影投影脚下地面，地面敌人贴地不变；camp_01 妖怪 route 68 脚底恒定 0.92；版本标记 v20260815b→c
- 【时间轴标记点】关卡进度条新增「打点1/打点2」双标记（黄/青双刻度+清除）：重播从最早标记开始、重置仍从 0、换关卡自动清空；两标记同时存在时在两点间循环播放（到后点回前点并复位片段：敌人重播/击杀重置/兔洞切路线还原，暂停拖拽不回绕）；版本标记 v20260815c→e
- 【重置缩放】点「重置」同时恢复关卡初始缩放（camHome 保存初始相机位姿 + resetZoom 恢复）；重播保持当前缩放；版本标记 v20260815e→f
- 【部署干员（参考 PRTS.Map）】新增 data/operator_table.json（425 干员：职业/稀有度/精英范围/技能名+技能开启范围+描述）与 data/range_table.json（73 范围模板）；「部署格」面板搜索干员→点可部署格放置（稀有度色圈+名字+朝向箭头）→点干员菜单调 4 向朝向、点技能查看开启后攻击范围（橙色高亮）、撤离；范围按官方 grids 约定随朝向旋转；换关卡清空；版本标记 v20260815f→g
- 【部署干员增强】面板新增职业/星级筛选 + 官方头像（Aceship avatars，失败回退色圈）；部署后场上生成干员 Spine 模型（isHarryh/Ark-Models 官方模型 425 名全覆盖，复用敌人 Spine 离屏管线，脚底贴地+稀有度底座圆环+白色朝向箭头，朝左镜像贴图）；修复模型 URL 前缀拼接 bug（build_char_char_ 重复）；版本标记 v20260815g→h
- 【全屏压扁修复】全屏后打开部署面板等动态内容压缩地图容器高度，画布缓冲不更新被 CSS 拉伸导致关卡内容垂直压扁；新增 applyCanvasSize() + ResizeObserver 监听 #mapWrap 实时同步画布与相机 aspect；版本标记 v20260815h→i
- 【干员模型尺寸与动画修复】模型缩放 0.9→1.25（约 1 格高）；动画不动根因：干员 BattleFront 的 Default 动画为时长 0 静态姿势，改为优先选有时长站立/待机动画（Relax/Idle/Move/Battle/Interact），且模拟暂停时干员模型也持续播待机动画；版本标记 v20260815i→j
- 【干员/敌人互相索敌攻击（演示，无伤害）】干员按当前攻击范围索敌最近敌人、切攻击动作（Interact/Move 兜底）并镜像面朝目标；敌人按官方 rangeRadius 索敌切 Attack 动画（同 id 共享模型任一攻击即播）；不改变血量/击杀/路线；版本标记 v20260815j→k
- 【敌人攻击范围显示】新增「敌攻范围」开关：点击敌人单独显示攻击范围+命中干员红圈+详情卡文案；实现对齐参考站（Enemy.ts drawAttackRangeCircle）——远程（applyWay=RANGED/ALL）画红色半径圆环（rangeRadius−0.25、贴地、depthTest:false、renderOrder=100、随敌移动），近战不画范围仅命中标记；enemy_index.json/.js 补入官方 enemyData.applyWay（2130 全量）；版本标记 v20260815k→m
- 【敌人攻击严格范围】攻击判定与显示圆环统一（远程=rangeRadius−0.25 有效半径、近战=相邻 1 格），圈外干员不攻击；原 rangeRadius+0.35/1.15 冗余阈值已移除；版本标记 v20260815m→n
- 【字体本地化】fonts.gstatic.com 的 woff2 在国内大量 404；新增 fonts/fonts.css + fonts/SpaceGrotesk.woff2（google/fonts SpaceGrotesk[wght].ttf 用 fontTools+brotli 转 48KB woff2，300-700 字重），Space Grotesk 本地化、中文改系统字体栈；31 文件（30 HTML + tailwind.css）批量移除 Google Fonts 链接与 preconnect；版本标记 v20260815n→o
