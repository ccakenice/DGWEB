# DGWEB 项目日志

> 来源：GitHub 克隆
> 仓库：https://github.com/ccakenice/DGWEB
> 源码目录：E:\WebProjects\DGWEB
> 主文件：index.html
> 部署地址：https://dgwebq.pages.dev/

---

## 项目规范（用户要求，必须遵守）
- **版本同步优先（随时确认最新版本）**: 每次改动前先确认 `E:\WebProjects\versions\` 下最新版本（最高版本号）为工作目标；核对最新版本目录的项目文件是否完整（与主目录对比）；**完整则只在最新版本修改并同步**；**不完整则先从主目录补齐文件到最新版本，再在最新版本上修改**；完成后同步更新 `AI_PROGRESS.md`、最新版本 `版本说明.txt`

## 开发日志

### 2026-08-15
  - **修正：S2 积雪扩散逐层推进至最多 20 格**（Codex，V0.30）：用户反馈「初雪二技能期间积雪会扩散到最大20格」；原实现扩散只走种子格相邻一圈（BFS 不把新格入队）即停止，无法随时间推进到 20 格上限；修正——超过 5 层的部分累计为扩散次数（每超一层向外扩一格），BFS 将扩散出的新格继续入队逐层向外推进，最多向外扩散 20 格；本地 Playwright 验证——camp_01 S2 开启后扩散逐层增长至 5 范围格+20 扩散格=25 格封顶、之后不再增长；纯天赋仍不扩散；版本标记 v20260815u→v
  - **干员攻击判定与 Spine 解耦 + 敌人受击闪红**（Codex，V0.30）：用户反馈「初雪攻击不播放攻击动画、敌人也没有受攻效果」；根因一——干员攻击判定整段被 `if (!op.spineAnim) return` 挡住，Spine 模型未加载（或加载慢）时干员完全不进入攻击逻辑；修复——攻击判定与受击反馈不依赖 Spine（模型未加载也照常攻击），动画/朝向仅在模型存在时执行；根因二——模拟器无受击效果，新增受攻闪红：干员攻击命中目标时目标短暂染红 0.18s（约每秒一次模拟攻击间隔，纯视觉不改数据）；特效数据说明——官方 gamedata（Kengxxiao）只有 JSON 配置无 VFX 数据，特效在客户端/社区资源（isHarryh/Ark-Models、serine-qing/MapSimulatorBackend），受击以染色近似；圣聆初雪模型动画=Default(0s)/Interact/Move/Relax/Sit/Sleep，攻击用 Interact 兜底；本地 Playwright 验证——无模型时 attacking 正常、命中敌人闪红（redMeshes=1）；有模型时 Relax↔Interact 切换正常；版本标记 v20260815t→u
  - **圣聆初雪 天赋+二技能实装 + 干员技能开关**（Codex，V0.30）：应要求实装圣聆初雪（6★术师 char_1046_sbell2）二技能与天赋：天赋1「无垢的雪景」=范围内地面每10秒+1层积雪、每层移速-10%（最多5层）；二技能「霜涛覆岭」=开启后每2秒额外+1层、超5层向周围扩散（最多20格）、离开积雪5秒寒冷；干员菜单新增「开启技能/关闭技能」按钮（作用于当前选中技能，底座环变蓝提示）；积雪以蓝色半透明地面+「雪N/冻」标签显示；确定性模型（由部署/开关时间推导，拖拽回看一致）；无HP系统故雪上20%攻击力DPS以减速/冻结表现，天赋2（受击触发）暂不生效
  - **修正：冻结仅作用于蓝门 + 积雪不上高台**（Codex，V0.30）：用户反馈「只有雪蔓延到蓝门上的时候冻蓝门吧，不在蓝门上不会冻」「积雪不会占高台，只在地面蔓延」；已修正——冻结仅蓝门（保护目标点，tile_end）格积雪达5层时生效（其上敌人停止），其余地块最多5层只减速50%不冻结；积雪只在地面蔓延，跳过 HIGHLAND 高台与墙/禁行/围栏等非地面格（含扩散）；本地 Playwright 验证——camp_01 部署位范围13格含8高台→仅5地面格积雪；纯天赋 t=50 满5层 frozenTiles=0（蓝门未覆盖）、5层格敌人移速50%（dur 10.53 vs 5.26）；S2 扩散至9地面格仍不冻结；蓝门在范围内时 frozenTiles=1（仅蓝门、其余5层格不冻、冻标签1个）；近战阻挡回归通过；版本标记 v20260815s→t
  - **近战攻击改为「被阻挡才攻击阻挡者」**（Codex，V0.30）：用户反馈「寻路者战士也在打初雪」，并提议近战应写成被阻挡才攻击阻挡它的单位——确认为真实游戏语义并实现；新增 meleeBlocker——近战（地面）敌人只攻击其行进方向下一格/当前格的干员，干员必须放在敌人路线上才会被攻击，放在路线旁（路过）不打；飞行近战不受阻挡保持相邻格判定，远程逻辑不变；敌攻范围命中显示同步为只标阻挡干员，详情卡文案改「近战 · 被阻挡时攻击阻挡者」；顺带移除已无引用的 enemyAttackRadius；本地 Playwright 验证——camp_01 史莱姆干员放路线上→接近（d=1）与重叠（d=0）时攻击（命中格=1）、干员放路线旁（距离 1 格路过）→0 次攻击；版本标记 v20260815q→r
  - **信件机制敌人不直接攻击（惊艳事热衷者/美食的奴隶）**（Codex，V0.30）：用户反馈 TO-EX-2 这两个敌人仍会打到干员；PRTS 机制核对——惊艳事热衷者=首次攻击在目标所在格投放<惊喜信件>，美食的奴隶=吞食<惊喜信件>、信件失效时爆炸，均不直接攻击干员（官方 enemy_database applyWay 为 MELEE 但实际为信件机制，模拟器不模拟信件）；修复——map-sim.html 新增 NO_DIRECT_ATTACK_IDS（好外号热衷者/惊艳事热衷者/“美食的诱惑”/“美食的奴隶”4 个 id），enemyRangeCells 对这些敌人返回空 → 不显示命中范围/红圈、不触发攻击动画，敌攻范围详情卡标注「信件机制（不直接攻击）」；本地 Playwright 验证——TO-EX-2 贴脸（d=0/1）attacking 恒 false、普通近战「戴面具的孩童」对照照常攻击（20 次、min_d=1）、远处同 id 不误攻、无 JS 报错；版本标记 v20260815p→q
  - **敌人攻击严格范围实装（修复近战远处误攻）**（Codex，V0.30）：用户反馈「近战还在打很远处的干员」；根因定位——同 id 敌人共享一个 Spine 骨架与画布，updateCombat 中「同 id 任一在攻击 → 整个 id 播 Attack」使远处同 id 敌人也显示攻击动画；修复——每个敌人 id 新增独立 Attack 骨架/画布（仅该 id 有敌人在攻击时渲染，复用共享 WebGL 上下文管线），每个敌人按自身 attacking 状态在移动/攻击贴图间切换，远处敌人保持移动/待机；敌人索敌判定与显示圆环/命中标记统一走 enemyRangeCells（近战=相邻 4 格、远程=有效半径内格），attack/显示/命中三处口径一致；本地+线上 Playwright 验证：camp_01 同 id 史莱姆 t=10 时 6.08 格处敌人 attacking=false 且不显示攻击画面、1 格内敌人正常攻击（0 违例）；线上真实 Spine 下 4 类近战敌人 81 次可见检查 attacking==showsAttack 0 违例、独立 Attack 画布 12065 像素正常渲染；版本标记 v20260815o→p，commit cb3aff08
  - **字体本地化（修复 Google Fonts 404）**（Codex，V0.30）：用户反馈 k3kxo84…woff2 大量 404——是 fonts.gstatic.com 的 Noto Sans SC/Space Grotesk 分片（国内被墙）；方案：Space Grotesk 本地化（google/fonts 仓库 SpaceGrotesk[wght].ttf 136KB，fontTools+brotli 转 woff2 48KB，300-700 全字重），中文改系统字体栈（PingFang SC/Microsoft YaHei）；新增 fonts/fonts.css，31 个文件（30 HTML + tailwind.css）批量删除 Google Fonts link/preconnect 并替换字体栈；全站彻底无 googleapis/gstatic 依赖。本地+线上 v20260815o 验证（仅本地 fonts.css+woff2、0 404、Space Grotesk loaded），commit 7fc9a002
  - **敌人攻击严格范围**（Codex，V0.30）：用户要求"干员不在敌人攻击范围里则不攻击"；将敌人攻击判定与显示圆环统一——远程=官方 rangeRadius−0.25（有效半径，碰撞补偿，与参考站圆环一致），近战=相邻 1 格；移除原 rangeRadius+0.35 / 近战 1.15 的冗余阈值（会把圈外干员误判）；attack（updateCombat）、显示（renderEnemyRange）、命中检测（enemyRangeCells）三处统一；详情卡标注有效半径。本地+线上 v20260815n 验证（53 例范围内正常、12 例圈外不再误伤、0 违例），commit 2cc91be7
  - **敌人攻击范围对齐参考站**（Codex，V0.30）：应要求调研参考站（serine-qing/MapSimulator Enemy.ts）——攻击范围是远程敌人（isRanged=applyWay RANGED/ALL）的**红色半径圆环**（EllipseCurve+THREE.Line，半径=getPixelSize(rangeRadius−0.25)，depthTest:false，renderOrder=100，position.z=0 贴地），近战不显示；按此重构本站显示：远程画红圈随敌移动、近战不画范围仅保留命中干员红圈与名单。数据核对：官方 enemy_database.json 的 enemyData.applyWay/rangeRadius（applyWay 在 enemyData 层，非 attributes），发现 rangeRadius>0 与 applyWay=RANGED 有 313 个不一致，故将官方 applyWay 补入 enemy_index.json/.js（2130 全量，0 缺失）；rangeRadius 全量 2130/2130 一致。本地+线上 v20260815l/m 验证（莫科克 RANGED/1.75 红圈、史莱姆 MELEE 无圈+命中标记），commit 94b13c37
  - **敌人攻击范围显示**（Codex，V0.30）：新增「敌攻范围」开关；开启后点击敌人单独显示攻击范围（红色半透明地块，敌人移动实时跟随）+ 范围内已部署干员红圈标记 + 敌人详情卡「攻击范围 · 官方 rangeRadius」与可命中干员名单；关闭开关/关详情/换关卡清除。数据核对：官方 enemy_database.json 的 enemyData.rangeRadius（注意不在 attributes 里），全量比对 2130/2130 与本站 enemy_index 完全一致（0 误差）；正值=半径格圆形、≤0=近战相邻 4 格。本地+线上 v20260815l 验证（史莱姆近战 4 格命中银灰），commit 3f8d42fe
  - **干员/敌人互相索敌攻击（演示，无伤害）**（Codex，V0.30）：干员按当前攻击范围（模板+朝向旋转，opRangeCells 缓存）索敌范围内最近敌人，切攻击动作（干员 BattleFront 无 Attack，用 Interact/Move 兜底）并贴图镜像面朝目标（不改变用户朝向箭头/范围）；敌人按 enemy_index levels[0].rangeRadius 索敌（近战 -1→1.15 格），同 id 共享模型任一攻击即播 Attack，离开范围交还 WAIT/移动逻辑（WAIT 切换加攻击态保护防每帧重置）；纯演示不产生伤害。本地+线上 v20260815k 验证（银灰放路线中段 t=20 双方索敌：opAnim=Interact/enemyAnim=Attack），commit bd79acaf
  - **干员模型尺寸与动画修复**（Codex，V0.30）：用户反馈模型太小且不动；模型缩放 0.9→1.25（约 1 格高）；排查动画不动根因——干员 BattleFront 模型 Default 动画时长 0（静态姿势，骨骼各时刻位置不变），pickOperatorAnimation 改为优先选有时长的站立/待机动画（Relax/Idle/Move/Battle/Interact），并让 opSpines 在模拟暂停时也以 0.016 步进播放（规划标记语义）。本地+线上 v20260815j 验证（银灰 1.25、Relax 动画暂停/播放均持续变化），commit c8d0b222
  - **全屏压扁修复**（Codex，V0.30）：用户反馈全屏后关卡内东西被压扁；复现定位——全屏中打开部署面板等动态内容使 #mapWrap 高度从 924 缩到 581，画布缓冲仍 924 被 CSS 100% 拉伸垂直压扁（缓冲/显示错位）；新增 applyCanvasSize()（setSize+aspect+投影同步）并用 ResizeObserver 监听容器，任何尺寸变化实时重设缓冲；本地+线上 v20260815i 验证（全屏→开面板 1882×581 同步→收面板恢复→退出恢复），commit ea7bbe56
  - **部署干员增强（筛选/头像/场上模型）**（Codex，V0.30）：部署面板新增职业（8+全部）与星级（1~6+全部）筛选；干员列表显示官方头像（Aceship avatars，onerror 回退稀有度色圈）；部署后场上生成干员 Spine 模型（isHarryh/Ark-Models，425 名基础皮肤全覆盖，复用敌人 Spine 离屏渲染管线：opSpines 渲染 + 脚底贴地 + 稀有度底座圆环 + 白色朝向箭头，朝左镜像贴图，加载失败回退圆环）。排查并修复模型 URL 前缀重复 bug（build_char_char_）；本地+线上 v20260815h 验证（医疗筛选 38、银灰模型 20798 像素、朝向旋转/镜像、撤离），commit f68ba2bb
  - **部署干员（参考 PRTS.Map）**（Codex，V0.30）：新增 data/operator_table.json（官方 character_table+skill_table 精简，425 名干员含技能范围）+ data/range_table.json（官方 73 模板）；「部署格」打开面板：搜索干员→点可部署格放置（稀有度色圈+名字+朝向箭头）→点干员菜单调 4 向朝向/点技能看开启后范围（橙）/撤离；范围按官方 grids（col=前方、row=侧向）随朝向旋转，换关卡清空。修正 facingOffset 朝向约定（初版 row/col 弄反）；本地+线上 v20260815g 验证（银灰默认 7 格、真银斩 3-12），commit af183eb2
  - **重置缩放**（Codex，V0.30）：点「重置」同时恢复关卡初始缩放——build3D 保存 camHome（初始相机位置/目标/up），resetZoom 恢复相机位姿与投影；「重播」保持当前缩放不重置。本地+线上 v20260815f 验证（拉近后重置精确回初始位姿），commit d4133a1a
  - **标记点2 + 两点循环**（Codex，V0.30）：「打点1/打点2」双标记（黄/青双刻度）；「重播」从最早标记开始；两标记同时存在时播放到后点自动回前点循环（复位片段状态：敌人重播/击杀重置/兔洞切路线敌人 _baseRoute 还原；暂停拖拽不回绕）。本地+线上 v20260815e 验证（90↔100s 循环多次回绕），commit 7b7df996
  - **时间轴标记点**（Codex，V0.30）：关卡进度条新增「打点/清除」按钮与黄色标记刻度；「重播」从标记点开始（已过敌人按进度定位），「重置」仍从 0，换关卡自动清空标记。修复 updateMarkerUI 作用域嵌套 bug（bindUI 未闭合导致顶层 loadStage 调用不到）；本地+线上 v20260815d 验证通过，commit 15e0c71e
  - **飞行敌人高度修复**（Codex，V0.30）：飞行敌人原与地面单位共用 tileSurfaceY 逐帧贴地，路线经过高低台时上下起伏；改为路线构建时计算 routeFlyY（全程最高地形顶 + 0.25，检查点+线段 0.25 格采样覆盖墙顶），飞行脚底恒定、阴影投影脚下地面，地面敌人贴地逻辑不变。camp_01 妖怪 route 68 脚底恒定 0.92，本地+线上 v20260815c 验证通过，commit 9d2928d0
  - **官方配色回退**（Codex，V0.30）：用户反馈官方固有色纯色平涂太丑、无场景模型衬托；撤掉 TILE_COLOR_URLS 与 init 覆盖逻辑，恢复 V0.29 深色 TILE_COLORS（road #333333 / wall #4b4b4b / floor #202020）；tile_colors.json 保留作数据参考不再应用；本地验证 v20260815b，已推送 GitHub（commit b6741f44）
  - **V0.29 定格 + V0.30 创建**（Codex）：经用户确认将 V0.29 定格（版本说明状态→定格），复刻 DGWEB_V0.30（草稿）继续开发；AI_PROGRESS 版本表/同步流程/待办同步更新
  - **官方地块固有色提取**（Codex）：官方 gamedata（Kengxxiao 全仓库）确认无地块配色字段；固有色取自参考站 MapSimulator 源码 tileTextures 硬编码色 + 官方地图纹理图集 tiles1.png（6×5 单元格）采样；官方无专色的 creep/mire/rope 等 9 种走参考站 getTile 默认规则。生成 data/tile_colors.json（61 条，本地 36 种关卡地块全覆盖，标注 ref_solid/atlas/ref_default 来源）
  - **地图模拟官方配色上线**（Codex，V0.30）：map-sim.html 新增 TILE_COLOR_URLS，init 时加载 tile_colors.json 覆盖默认深色 TILE_COLORS（失败保留兜底），红蓝门/绿门用官方描边色保持可见；本地+线上验证 v20260815a（0-1 渲染采样到 #747474/#c1c1c1），已推送 GitHub（commit 2558e42b）

### 2026-08-14
  - **模型裁剪修复**（Codex）：Spine 离屏画布 128→256 升级时漏改行拷贝长度——putImageData 前每行只复制 `src+512` 字节（128 像素），256 画布右半恒透明，模型视觉上「被裁剪成一半/半透明」；修复为 `src+1024` 整行复制。本地 Playwright 验证 19 敌人 + 3 道具 Spine 全宽渲染（enemy_1212_mtrfol bbox [48,32,127,218]→[48,28,252,217]）；版本标记 v20260814f→g
  - **敌人模型线上修复**（Codex）：Spine 资源源改从 GitHub 公开仓库（serine-qing/MapSimulatorBackend public/）经 jsDelivr/raw 加载；/spine 代理仅留数据接口。排查过程：CF Worker fetch 裸 IP 被 1003 拦截 → sslip/nip 域名被参考站防盗链按 Host 白名单拦（566/302）→ cloudflare:sockets 收 0 字节 → resolveOverride+localhost 被 CF 拦 403；最终发现参考站后端开源在 GitHub，资源直连解决。线上验证：默祷圣祠 19 种敌人 68 个资源全 200、401/401 应用；0-1 7 个资源全 200、11/11 应用
  - **回退占位贴地修复**（Codex）：用户反馈模型悬浮；核查确认 Spine 模型本身贴地（401/401、脚底偏差 0），悬浮的是 Spine 加载失败时的头像/圆形回退占位（居中贴在 1:1 平面）；改为内容底部对齐画布底部 + 缓存命中路径修复，实测头像/圆标内容底均到 127/128
  - **头像回退移除**（Codex）：按用户要求删除 Spine 失败时的头像立绘回退，仅保留位置标记（圆标+阴影）；圆标底部对齐画布底（127/128）
  - **脚底锚点改全程最低点**（Codex）：对照参考站源码（SpineEnemy.ts：根节点锚地 + MAP_ROTATION，-0.15 为行向深度偏移非下沉）；0-1 剑士跑动帧脚底浮动 8 行导致悬空，现循环内取最低点锁定；GROUND_SINK 0.08→0.02 修复压扁
  - **跑动空隙压平**（Codex）：0-1 剑士跑动帧脚底离地 0.05 格在高分辨率呈 2-3px 空隙；GROUND_SINK 0.02→0.045 压平，深放大验证贴地+无压扁
  - **身体后仰补偿**（Codex）：定位错误根因=纸片倾斜使身体偏移到格子后方；新增 modelLeanShift 前移补偿，身体对齐格心；参考环逐敌显示；验证脚贴环、身体居中
- **V0.29 全量部署上线**（Codex）：单次提交（GitHub Git Data API，commit 18e0722）推送 main 分支，CF Pages 自动构建生效，线上已可访问
  - 上线内容：map-sim.html 3D 地图模拟 / enemy.html 敌人图鉴 / data 全部索引与 36 个剿灭关卡本地 JSON / functions/spine/[[path]].js 同源代理 / js/spine 3.8 库 / images/map 官方地图贴图 / data/highland_colors.json 高台配色
  - 补齐 camp_r_20（千嶂边城）、camp_r_23（燃烧街区）本地关卡 JSON（此前为 0 字节空文件，本地优先加载会失败）；同步主目录与 V0.29 的 AI_PROGRESS / 版本说明 / latest.json / ark_pv.json / highland_colors.json
  - 清理仓库内 ai_handoff 旧副本（3 个文件）；线上验证：map-sim/enemy/data 均 200，/spine 与 /pv 函数路由生效，首页含「地图模拟 MAP」入口

### 2026-08-13
- **能力/描述就近展示**（Codex，V0.29 草稿）：顶部能力/描述加「能力 ABILITY」「描述 DESCRIPTION」标签；天赋参数卡内直接嵌入「能力/描述：…」文本，无需再回顶部对照
- **天赋展示调整**（Codex，V0.29 草稿）：官方 enemy_database 无天赋中文名（仅内部 ID + talentBlackboard），区块改名「天赋参数 / TALENT PARAMS」，ID 以 chip 呈现并注明「含义见上方能力/描述」，参数保留中文标签+单位
- **敌人详情页技能/天赋参数美化**（Codex，V0.29 草稿）：
  - 天赋按前缀分组展示（如 ThrowBomb），参数转中文标签 + 单位（间隔/攻击力倍率/动画时长→s/%）；原始 key 放悬停 title
  - 修复 `formatParam` 倍率正则把 `duration`（含 `ratio` 子串）误判为百分比的问题（锚定结尾）
- **敌人详情页补全技能/天赋**（Codex，V0.29 草稿）：
  - 新增 `data/enemy_detail.json/.js`（替代 enemy_desc）：合并官方 enemy_database（描述/移动/攻击方式/技能 blackboard/天赋 talentBlackboard/范围/目标值）与 enemy_handbook_table（能力列表/等级类型/伤害类型）
  - 详情页新增「基本信息」「技能」（prefabKey + 优先级/初始冷却/冷却/SP 消耗 + 参数表）「天赋」（参数表）区块，807 个敌人有技能、1446 个有天赋、1583 个有能力文本；修复 fmt 对数组/字符串值的兼容
- **敌人详情页**（Codex，V0.29 草稿）：
  - 新增 `enemy.html` 敌人图鉴详情页：`?id=` 直达，支持 ID/名称搜索（2130 敌人），展示头像、名称、ID、官方描述（来自 enemy_database.json，剥离 `<@>`/`<$>` 富文本标签）、各等级属性（HP/ATK/DEF/RES/SPD/ATK SPD/攻击间隔/重量）
  - 新增数据 `data/enemy_desc.json/.js`（2130 条，1910 条有描述）；地图模拟敌人详情卡加入「查看详情」按钮跳转详情页
- **敌人详情菜单**（Codex，V0.29 草稿）：
  - 复刻参考站（lutonada）：点击地图上的敌人弹出详情浮窗（`#enemyDetail`），含头像、名称、ID、类型（地面/飞行）、波次·路线·进度、六维属性（HP/ATK/DEF/RES/SPD/WT）
  - 弹出时自动暂停并高亮该敌人路线；点击空白处或 × 关闭（同时隐藏路线）
- **敌人路线交互**（Codex，V0.29 草稿）：
  - 参照参考站（lutonada）：所有敌人路线默认隐藏（`state.routeVisuals` 按路线索引收集线/点/终点标记）
  - 点击地图上的敌人：自动暂停播放并显示该敌人当前路线（射线拾取可见敌人 Sprite）；点击空白处隐藏全部路线；图例区加「点击敌人查看其路线（自动暂停）」提示
- **地穴实装**（Codex，V0.29 草稿）：
  - 官方数据里 `tile_hole` 的 `passableMask` 为 ALL（可通行），此前一律当阻挡；改为按掩码判定后，TO-EX-2 地穴（左侧 3×2 洞区）成为可通行洞穴，3 只「酸橙猴」按官方波次从洞穴格（routes 13-15）出生并沿洞内路线走出
  - 路线起点改为按官方 `startPosition` 出生（地穴洞口、传送门出口、地图中段），不再吸附红门；0-1 等主线实测不受影响（已用路线起点即红门）
  - 洞口渲染为黑色圆坑（参考站同款）；顺带修复 A* 路径简化循环在相邻格访问差 >1（如禁行格→传送门出口）时 `optimized.push` 死循环导致「Invalid array length」的隐患（改为保证循环前进的贪婪简化 + 父链防环 + 重建兜底）
  - 按 `motionMode` 区分敌人运动：地面（WALK）敌人走进 `tile_hole` 直接掉坑死亡（参考站同款 hp=0，计入击杀）；飞行（FLY）敌人按路线检查点直飞折线，无视高台/墙/禁行/地穴（routes 13-15 猴子出洞、16-26 飞兵过洞均为 FLY）
- **活动传送门机制（兔洞）**（Codex，V0.29 草稿）：
  - 官方数据：`tile_rabbithole_in_<n>` 入口、`tile_rabbithole_out_<n>` 出口（blackboard 带 `action_index` 与 `prob` 权重）、`extraRoutes` 出口路线、`branches.rabbit_hole_route`；参考站（lutonada）实现为敌人进洞后消失 3 秒，按出口权重随机出现并走对应 extraRoute
  - map-sim 新增：`buildRabbitHoles` 解析入口/出口并算概率百分比；`buildRoutePaths` 支持 `rawStart` 把出口路线（extraRoutes）追加到路线池（`extraRouteBase + action_index`）；`buildEnemyMeshes` 预计算敌人首次踩到入口的进度与确定性出口选择（拖拽回看不跳变）；`updateEnemyPositions` 按时间实现「进洞消失 3 秒 → 出口出现 → 走出口路线到蓝门」
  - 出口路线按官方检查点折线行走：左上「出2」出口（extraRoutes 5/6）的猪会沿洞区走进地穴被击杀（不会瞬移穿过地穴）；其余出口沿道路直达蓝门
  - 传送门地面标记：入口「入<n」深棕圆、出口「出<n + 概率%」绿圆（参考站同款样式，256px 高清标签）；图例区追加「传送门 · 出口概率」列表（每个出口坐标 + 概率%）
  - TO-EX-2 实测：2 个入口 / 7 个出口（各 14.3%），82 只猪全部触发传送，进洞隐藏、3 秒后从出口路线出现；TO-EX-8（信箱关）回归无影响
- **场地道具渲染**（Codex，V0.29 草稿）：
  - 关卡 `predefines.tokenInsts` 里的预置部署物（道具）此前完全未渲染；以「直到大地变成一颗酸橙」的信箱（`trap_334_agmbox`，TO-EX-8 位于 (5,7)）为切入点补齐
  - 与参考站（lutonada）一致：`POST /assets/getTrapsKey` 取道具数据，spine 类型用 `trap/spine/<skel>/<skel>.skel + .atlas` 官方动画（播放 Trap_Idle），image 类型用 `trap/image/...` 静态贴图，fbx 类型暂跳过
  - map-sim 新增 buildTrapMeshes / prepareTrapSpines / applyTrapMeshes：Spine 离屏渲染管线复用敌人同套实现，道具脚底贴地、比例与敌人同公式（默认约 0.9 格）；加载前显示半透明占位标记
  - `functions/spine/[[path]].js` 代理扩展：支持 `assets/getTrapsKey`（POST）与 `trap/spine`、`trap/image` 路径（HTTPS 线上可用）
  - 已同步 DGWEB_V0.29（map-sim.html、functions/spine/[[path]].js，哈希一致）
- **选关「活动」模式活动名修正**（Codex，V0.29 草稿）：
  - 原活动列表直接使用 zone 子区域名（如「钳兽牢笼」「驶向大地尽头」）并按中文排序，部分 zone 显示英文 zoneId
  - 重新核对官方 activity_table.json：`basicInfo` 含全部活动官方名与开始时间，`zoneToActivity` 建立 zone→活动 映射；已核对 PRTS 活动一览交叉确认（最新「奇象巡展」2026-08-08）
  - 活动名改用官方活动名（子区域名降级为副标题），按开始时间倒序，最新活动置顶；隐藏「不展示」的 RPG 关卡区（act53side_zone4）
  - 重新生成 data/zone_index.json/.js 并同步 DGWEB_V0.29（哈希一致）

### 2026-08-11
- **版本回退保障落地**：新增 `c_drive_mirror\tools\version-mgmt\`（manifest / 版本清单 / 完整性校验 / 回退脚本），全部历史版本已生成 `manifest.json`；新增 `versions.json` 作为版本状态单一事实源
- **Codex 会话文档**：每个新版本自动生成 `codex-会话.md`（版本状态、改动说明、动态数据时间），管理器「提交版本 / 定格版本」已接入 `Sync-DGWEBVersionMeta.ps1`
- **地图模拟页 map-sim.html**：按 DGWEB 风格新增单页 3D 关卡地图模拟，Three.js 渲染 3D 地形、路线与敌人波次推进；数据来自 ArknightsGameData，本地索引 `data/stage_index.json/.js` + `data/enemy_index.json/.js`
- 接入官方美术资源：关卡「官方地图」预览（yuanyan3060/ArknightsGameResource map + fexli mapreview 兜底）、敌人头像（jsDelivr 优先 + raw 兜底）
- 视觉精致化：ACES 色调映射 + 软阴影 + 半球光/轮廓光、地面受影、路线点阵光带、敌人 Sprite 贴图（失败回退圆形标记）
- 红蓝门方向明确：敌人从红门出生（`gate_red.png`）、进入蓝门消失（`gate_blue.png`）
- 场景贴图优化：接入官方 `tiles1` 图集，地板/草地/楼梯/传送点/火山/治疗等格子使用官方顶部贴图
- 路线数据修复：只使用 `MOVE` 检查点，忽略 `WAIT_FOR_SECONDS` 的 `(0,0)` 占位，避免路线乱跳
- 路线寻路：使用明日方舟 Stage-Viewer 同款 A* + 视线优化，路线起点对齐红门、终点对齐蓝门，不再穿墙
- 敌人动画：场景内敌人从静态头像改为官方 Spine 3.8 动画，WebGL 离屏渲染后贴入 3D 场景，加载失败自动回退头像
- 修复动画透明/闪烁：WebGL 帧经 `readPixels` 快照到 2D 画布，并按加载时固定比例和中心渲染，避免动画过程中忽大忽小
- 修复模型垂直翻转：动画贴图恢复默认 `flipY`，敌人头朝上显示
- HTTPS 适配：新增 `functions/spine/[[path]].js` 同源代理，线上通过 `/spine/` 访问参考站动画资源，避免混合内容拦截
- 官方地图预览保留在独立弹窗；「叠图」改为与地图格子严格对齐的示意底图，不再把透视截图的官方地图平铺到 3D 地面造成歪斜
- 修复 3D 模型与底图错位：地形/路线/敌人坐标统一到地图坐标系
- 锁定相机旋转与拖拽平移，仅保留滚轮/双指缩放
- 使用官方相机数据（fexli map_camera_views/summary.json 生成 data/camera_index.js）设置官方视角并锁定
- 修正官方视角轴换算：统一使用 `view_default`，Unity Y/Z 映射为 Three.js 深度/高度，改为高视角 3/4 视图，避免 `view_by_side` 将部分关卡带成侧视歪斜
- 修复红蓝门左右镜像：对照参考站确认 `0-1` 红门右、蓝门左，通过相机投影矩阵 X 镜像校准；缩放保留，旋转锁定不变
- 首页导航新增「3D 地图模拟 MAP」入口；DGWEB_V0.29 已同步并重新生成 manifest / codex-会话.md

### 2026-08-10
- **社区工具修正**：底部工具栏「图米米」更名「特米米」（链接不变）；「公招计算」跳转由 /w/公开招募 改为 https://prts.wiki/w/公招计算（%E5%85%AC%E6%8B%9B%E8%AE%A1%E7%AE%97）
  - 修改 26 个页面（index/modes/mode-*/preview_*/modes_prev_*/detail/stage/search/ranking/aliases/theme-videos 等），已同步 V0.26

- **管理器「定格+复刻」提速修复**（软件开发管理器_V0.01）
  - 根因：管理器目录含 260MB/2978 文件（_cr* 残留 148MB、shots 52MB、_clone_dgweb、versions 嵌套备份 25MB 等），robocopy /E 全量复制且每次定格越滚越大
  - 修复：4 个脚本 8 处 robocopy 增加 /XD 排除（.git __pycache__ backup_* shots _cr* _clone_* versions WebProjects res_screen）
  - 效果：管理器项目复制从 260MB/2978 文件降至 13MB/239 文件，约 0.8s 完成（DGWEB 281MB 本只需 1s，不受影响）

- **首页背景 PV 变糊修复**（quality 1080P->480P）
  - 根因：BILI_SESSDATA 过期，2026-08-10 21:34 起抓取 quality 由 80 降为 32（B站游客仅 480P）
  - fetch_ark_pv.py 新增质量降级保护：新 quality 低于上次发布时保留旧 stream 并输出 WARNING
  - 已更新 deploy-config.json 的 BILI_SESSDATA（用户提供新值），重抓恢复 quality=80，GitHub 双仓库（bili-sync/DGWEB）上传 OK
  - 已按「版本同步优先」规范同步 fetch_ark_pv.py / ark_pv.json / deploy-config.json / AI_PROGRESS.md 至最新版本 DGWEB_V0.26

### 2026-08-11
- **核心使用率 + 别名干员头像失效修复**（ranking.html / aliases.html）
  - 根因：原引用 media.prts.wiki 头像 URL（hash 目录本地推导），批量检测 239 个去重 URL 中 98 个返回 404（约 41%，如 水陈/圣葬/叔叔/羊/浊蒂 等），头像不显示。
  - 方案：全部改用本地头像 images/ext/avatars/（141 张官方头像，文件名 a_[hash]_头像_干员名.png）。
    - ranking.html：AVATARS 239 条全部替换；别名键（M3/小羊/叔叔 等 98 个）经 ALIAS 表映射回官方干员名后指向本地官方头像。
    - aliases.html：ALIAS_OPS 136 条 avatar 全部换成本地路径。
    - 修复过程中发现替换丢引号 bug，已修复；JSON 解析验证通过，两个页面 0 个远程头像引用。
  - 已同步 ranking.html / aliases.html 至 V0.26，并更新 V0.26 版本说明。
- **版本目录核查与对齐（重要）**：发现 versions 目录存在 DGWEB_V0.27（08-10 23:36）与 DGWEB_V0.28（08-11 00:49）草稿版，此前误以为 V0.26 为最新。经核查两版均落后主目录（缺特米米/公招计算/头像修复）。已将主目录全量对齐同步至 V0.27/V0.28（各更新 175 个文件、清理 141 个旧编码头像名），并追加各自版本说明。**结论：最新版本为 DGWEB_V0.28**，后续改动以 V0.28 为同步目标。
    - 补充：线上头像仍 404 根因为存储文件名含字面 % 编码（服务器 URL 解码失配），已重命名 141 个文件为纯 hash 名 a_[hash].png 并更新 375 处引用，线上 141/141 验证为真实 PNG。


### 2026-08-07
- **C 盘项目文件全部迁入 E 盘**（目录重组）
  - `C:\DGWEB` 整目录（276.7 MB，3026 文件）迁至 `E:\WebProjects\DGWEB\c_drive_mirror\`，原盘目录已删除
  - 更新全部引用路径：`projects.json`（backupDir/logFile）、`manager.ps1`、`deploy.ps1`、`AI_DOCS.md`、`opencode_log.md`、日志等 60+ 文件
  - 计划任务 `DGWEB_DataFetchHourly` 的脚本路径同步更新（DGArkPVSync 本就指向 E 盘）
  - 安全加固：`c_drive_mirror/` 加入 `.gitignore`（内含 GitHub token / B 站 SESSDATA，禁止上传）
  - 管理器已从新路径重启运行；历史备份快照（backup_*/BEFORE_DEPLOY）保留原内容不改

### 2026-08-06
- **V0.22 hero 官方先导PV全屏背景（草稿，待定格）**
  - fetch_ark_pv.py：WBI 签名搜索明日方舟官方空间(161775300)「先导PV」最新视频 → ark_pv.json（已验证抓到 BV1KN3M6wEm9「直到大地变成一颗酸橙」活动先导PV）
  - 主页 hero 全屏背景 iframe 播放器：无 UI 沉浸式（无按钮/标签/边框/进度条）、t 随机起点(0:07~2:30)、定时重载模拟循环、默认静音自动播 + 首次交互自动开声、暗色渐变遮罩保证文字可读
  - 用户反馈改版：初版为右上角小副屏卡片+静音按钮/标签 → 改全屏背景无UI（面积更大、去掉播放器UI）
  - daily.yml 增加 fetch_ark_pv.py 步骤（复用 BILI_SESSDATA 每日抓取）
  - 本地 playwright 验证通过（全屏覆盖、无UI、随机t、交互开声）
  - 已上传 GitHub（git push 遭网络阻断 → 改用 api.github.com Contents API 上传）

### 2026-08-06
- **V0.21 功能完成（草稿，待定格）**
  - 详情页分 P 播放器优化（detail.html）：p-nav 翻P + iframe src 同步 + URL `?p=N` + autoplay=1
  - 播放进度本地存储续播：localStorage `PLAY_KEY` 记住播放 P，再进入自动续播
  - 验证 B 站 postMessage 跨域通信：不可行，降级为「记住上次播放的 P」
  - 搜索高亮优化（search.html）
  - 干员筛选面板（ranking.html，按名字/别名过滤榜单）
  - 移动端适配补强（detail/search/ranking）
  - 同步 V0.21 + 上传 GitHub + 线上验证通过
- **项目规范新增**：「版本同步优先（随时确认最新版本）」加入 AI_PROGRESS.md 项目规范（第 4 条）并同步到项目日志

### 2026-08-05
- **集成战略详情页重写** (mode-integrated-strategies.html)
  - 主题卡片改为固定比例缩略图（aspect-ratio 2.05/1，object-fit:contain）
  - 统一灰度滤镜（grayscale(0.4) contrast(0.95)），hover 恢复彩色 + 放大，与 modes 列表页视觉一致
  - 移除 zone-card 分支与多余覆写 CSS，保留 act-card 单一卡片类型
  - 修正顶部错误标注：Crisis Contract → Integrated Strategies
  - 无关卡主题显示「暂无收录关卡」
  - 已同步 V0.13，上传 GitHub 线上验证通过
- **AI 文档整理**: 重写 AI_PROGRESS.md（项目进度 + 规范 + 各功能实现逻辑），更新本日志供「理解AI」功能使用

### 2026-08-04
- 新增 3 个常驻模式：集成战略（6 主题）/ 保全派驻 / 生息演算，含详情页
- 集成战略主题自动同步脚本 sync_themes_auto.py（从 biligame wiki 提取）
- 底部工具栏「社区常用工具」上线（公招计算 / PRTS / 企鹅物流 / 一图流 / 图米米）
- 公招计算器独立页移除（recruit.html 删除，改跳转 PRTS）
- V0.12 定格

### 2026-08-03
- 干员泛用榜 ranking.html（总榜 + 年榜 + 统计规则折叠）上线
- 干员别名页 aliases.html 上线（136 干员，105 条别名并入匹配池）
- 修复泛用榜多处逻辑 bug（关卡代号正则、去重、剧情/TR 排除、剿灭豁免）
- V0.11 定格

### 2026-08-01
- 从 GitHub 克隆导入

---

## 待办

- V0.29（3D 地图模拟）待用户确认后定格

### 2026-08-12
- **文档一致性修正**（Codex）：
  - DGWEB_V0.27/V0.28 版本说明.txt 原为 V0.25 复制残留（版本号/日期/状态错误），已重写为各自草稿说明（V0.27=2026-08-10、V0.28=2026-08-11）并重新生成 versions.json
  - AI_PROGRESS.md 更新至当前状态（已定格 V0.25、当前工作版本 V0.29 草稿），AI_DOCS.md 最新版本说明由 V0.26 修正为 V0.29
  - 管理器 web「理解AI」项目日志路径修正（原指向不存在的 logs\DGWEB.md，改用 $p.logFile）；freeze_draft 草稿日期硬编码 2026-08-09 改为动态日期
  - V0.29 版本说明「来源：V0.28（已定格）」改为「V0.28（草稿）」（V0.28 未定格，与版本变更历史一致）
  - 已同步 ai_handoff 副本（AI_PROGRESS.md / 最新版本说明.txt / 版本变更历史.md / 项目日志.md）

### 2026-08-13
- **敌人详情页天赋/能力 PRTS 可读描述**（Codex）：
  - 用户指出天赋显示成 ThrowBomb 等内部 ID 不对、找不到能力/描述；以 PRTS 页面为准，为 act53side（小刻的追番小屋/酸橙争霸赛）9 种敌人补全 `prts_talents` 可读描述（如热心肠老板=「每秒从自身模型向自身所在位置发射披萨弹道，弹道命中时对半径0.45范围内所有我方单位造成攻击力100%的物理伤害（不可对空，无视迷彩）」）
  - 修正官方 desc 被裁剪的占位符（“大披风”=「夺取攻击范围内的<浮空信件>」、惊艳事热衷者=「在目标所在位置投放<惊喜信件>」），新增 `prts_desc` 字段；天赋参数卡内显示「能力/描述：…」，描述区优先展示 PRTS 可读文本
  - 已同步 E:\WebProjects\DGWEB\data\enemy_detail.json/.js 与 V0.29，本地验证 9 种敌人全部渲染正常
- **敌人卡片抗性信息 + 属性中文化**（Codex）：
  - 用户反馈卡片跳转麻烦、缺抗性、ATK 等英文；地图模拟点击敌人的浮窗卡片直接显示能力 chips、PRTS 可读描述/天赋文本、元素抗性/损伤抵抗/异常免疫，六维全部中文化（生命/攻击/防御/法抗/移速/重量/攻击间隔）
  - enemy_index.json/.js 重新生成，补充官方 attributes 的元素抗性（epResistance）、损伤抵抗（epDamageResistance）与 7 种异常免疫标记；enemy.html 详情页基本信息区同步新增元素抗性/损伤抵抗/异常免疫
  - 已同步 V0.29（map-sim.html / enemy.html / enemy_index / enemy_detail），本地 playwright 验证卡片数据完整、布局不越界
- **叠图升级为官方地图低透明底图**（Codex）：
  - 用户提出「把官方地图用低透明度盖在地图上」；地图模拟「叠图」按钮改为加载官方地图图片（map/act53side_ex02.png 等）作为 0.35 透明度平面，贴合 3D 格子区域（cols×rows）
  - 用户反馈层级不对（先要上层、再要别盖住敌人/道具/场景贴图）：最终层级为 3D 地形 < 官方地图（renderOrder=0.5）< 场景贴图/敌人/道具（1/4/5），路线提升至 renderOrder=3；官方地图低透明盖层不再遮挡场景内容
  - playwright 验证：纹理替换、透明度、叠图开关、层级（官方地图仅从场景贴图透明区域透出）均正常；图片加载失败自动回退原格子示意图
- **叠图透明度/缩放滑块**（Codex）：
  - 叠图开启时显示「透明度」「缩放」滑块（默认 35% / 100%），实时调整官方地图盖层透明度与平面缩放（以地图中心为基准），便于手动对齐参考图；关闭叠图自动隐藏，数值跨关卡保留
  - playwright 验证：透明度 0.35→0.7、缩放 1.0→1.5 实时生效，滑块显隐与叠图开关联动
- **叠图 X/Y 偏移滑块**（Codex）：
  - 用户反馈叠图对不上：官方地图图片为 320×180（16:9）场景缩略图，非纯格子图，无法可靠自动定位格子区域；新增 X/Y 位置滑块（±100%，幅度为格子区域半宽/半高，随缩放联动），配合缩放可手动对齐
  - playwright 验证：X +30 位移至 8.45、Y -20 位移至 3.6，偏移实时生效
- **格子边缘线改细**（Codex）：
  - 用户反馈 3D 高台的粗线/缝隙明显；格子边缘从整块立方体 12 条边（EdgesGeometry）改为只画顶面 4 条细分割线（深色 #171a1f、opacity 0.45、y 抬升 0.006），保留分割感、消除粗线
  - playwright 验证：0-1 场景所有格子线均为 5 顶点顶面闭合线，无报错
- **高台缝隙消除**（Codex）：
  - 用户反馈仍粗并问是否高台间缝隙；根因：格子宽 0.96/0.99 留下深色缝隙 + 顶面贴图仅 0.86 露出深色本体
  - 修复：格子 BoxGeometry 加宽至 1.0 完全贴合、顶面贴图加大至 0.98 铺满、顶面分割线降淡至 opacity 0.22；playwright 验证格子尺寸 1.0、贴图 0.98
- **缝隙微调**（Codex）：
  - 用户反馈完全无缝反而不好看；格子收窄至 0.985（留约 0.015 极细缝隙）、顶面贴图 0.955（保留贴图边缘分割感）
  - playwright 验证：TO-EX-2 格子 0.985、贴图 0.955，无报错
- **可部署地块高亮**（Codex）：
  - 用户要求仿干员技能范围预览：工具栏新增「部署格」开关，按 buildableType（MELEE/RANGED）在可部署地块叠加绿色 45° 斜线排线高亮（canvas 纹理），animate 中正弦脉动透明度（约 0.3~0.8）缓慢闪烁；点击开关显示/隐藏
  - playwright 验证：TO-EX-2 40 个可部署地块 mesh 创建、开关联动、透明度脉动生效，截图含绿色像素
- **剿灭作战全量实装**（Codex）：
  - 用户要求补全剿灭关卡并实装最新数据；根因：zone_index 生成脚本按 code 去重，而剿灭关卡 code 为地区名（同区多关 code 相同）导致每区只留 1 关
  - 修复：gen_zone_index.py 按关卡唯一 id（camp_02/camp_r_33 等）收录并输出 id 字段；「剿灭」模式现含全部 36 个剿灭作战（15 个地区分区，分区名「地区 剿灭」），含最新 r_32 朱墙食府、r_33 默祷圣祠；map-sim 选关点击改用 id 加载，findStage 支持 id 精确匹配
  - playwright 验证：剿灭模式 15 个地区分区、camp_zone_2 含朱墙食府，点击加载成功（9×13 地图、400 敌人）；r_33 默祷圣祠直接 ?id=camp_r_33 加载成功
- **剿灭模式全列**（Codex）：
  - 用户要求剿灭直接全列、不要活动/分区选择；renderZones 对 annihilation 模式隐藏活动下拉（zonePickRow），按 id 去重后直接平铺全部关卡（36 剿灭 + 周常资源关）
  - playwright 验证：剿灭模式 71 个关卡平铺、活动下拉隐藏、点击默祷圣祠加载成功
- **剿灭模式去除资源本/芯片本**（Codex）：
  - 用户反馈剿灭关卡不全且混入资源本/芯片本；gen_zone_index.py 的 annihilation 类型由 ["CAMPAIGN","WEEKLY"] 改为仅 ["CAMPAIGN"]
  - 重新生成后「剿灭」模式只含 36 个真正的剿灭作战（15 个地区分区），PR/LS/AP/CA/SK/CE 等不再出现；playwright 验证 36 关、无 PR-/LS- 等
- **剿灭列表排序与标题**（Codex）：
  - 用户要求关卡名大标题、地区小标题、按开放时间倒序；剿灭卡片改为「关卡名（大）+ 地区（小）」，按官方关卡 id 开放顺序（camp_r_33 最新 → camp_01 最早）倒序排列
  - playwright 验证：首位默祷圣祠/拉特兰，末位切尔诺伯格/乌萨斯，36 关完整
- **剿灭 URL 用关卡 id**（Codex）：
  - 用户反馈默祷圣祠白屏；根因：loadStage 后 history.replaceState 用 stage.code（地区名），同地区多关 code 相同，刷新时 findStage 命中错误关卡导致加载失败
  - 修复：URL 回写改用 stage.id（?id=camp_r_33）；playwright 验证点击默祷圣祠后 URL 为 ?id=camp_r_33、加载成功
- **剿灭关卡本地数据 + 白屏修复**（Codex）：
  - 用户反复反馈默祷圣祠白屏/黑屏；排查确认本地渲染正常、数据文件存在，白屏形态为「关卡数据加载失败」（jsDelivr CDN 不可达）
  - 修复：全部 36 个剿灭关卡 JSON 下载到 data/levels/ 本地（camp_01~camp_r_33），loadStage 加载顺序改为本地优先 + CDN 兜底；stage_index/enemy_index/enemy_detail/camera_index 等 script 加版本号 ?v=20260813c 防缓存
  - playwright 验证：camp_01/camp_r_33/camp_r_32/camp_r_01/camp_r_21 在 CDN 不可达时本地加载全部成功渲染
- **剿灭关卡白屏根治**（Codex，V0.29 草稿）：
  - 用户反馈默祷圣祠仍白屏；根因：Spine 敌人每类一个 WebGL 上下文，默祷圣祠 19 类敌人 + 主渲染器 = 20 个，超过浏览器 16 个上限，最早创建的主渲染器上下文被回收 → 3D 区白屏
  - 修复：全部 Spine 敌人/道具改用一个共享 WebGL 上下文（getSharedSpineRenderer），逐骨架渲染 + readPixels 快照到各自 2D 画布；上下文数 20→2，clearScene 统一释放
  - playwright 验证：默祷圣祠 19 种敌人正常渲染（mainContextLost=false、画布 20508 色）、TO-EX-2/0-1 无回归
- **敌人移速与罚站时间实装**（Codex，V0.29 草稿）：
  - 用户反馈默祷圣祠「雕像飞走了，没有罚站时间」；根因：模拟器所有敌人统一按 0.9 秒/格移动（忽略官方 moveSpeed），且路线 WAIT_FOR_SECONDS（5~120s）检查点被整体忽略——圣徒祈祷像移速 0.0 被当普通敌人送走
  - 修复：按官方 moveSpeed 计算移动耗时（移速 0 = 原地罚站，不移动不结算）；WAIT_FOR_SECONDS 的 (0,0) 占位按「当前位置罚站」解析挂到 path.waits；新增 routeTimeToProgress / routeProgressToTime，兔洞到达时间与出口路线同步计入
  - playwright 验证：圣徒祈祷像原地不动（progress=0、可见、位置在出生点）、90/120s 罚站冻结进度、maxTime 1697s 计入；TO-EX-2 兔洞（入口前罚站 190s 后传送、出口推进正常）、0-1 无回归
  - 补丁：用户反馈「圣徒祈祷像只剩半截」；根因=移速 0 敌人罚站分支提前 return 跳过逐帧脚底定位，精灵中心锚点停在地面高度（y=0.17），1.755 格身高下半 0.71 格入地；罚站分支补充 samplePath(0)+bodyBottom 贴地定位，验证雕像 y=0.91、脚底贴地、全身完整
- **场地机制核对与修复**（Codex，V0.29 草稿）：
  - 用户要求检查入梦砖/传送门/中立单位及其位置；核对默祷圣祠：11 种格子（含 tile_telin/tile_telout、tile_sleep_road/wall、tile_hole）、11 个预置单位（圣堂奶 trap_235/Milk×5、唤醒者 trap_237/waker、射手 trap_236/Shooter×5）
  - 位置 bug：tokenInsts 坐标与路线同为自底向上，buildTrapMeshes 漏 y 翻转，11 个单位垂直镜像错位（Milk 7 8 压 forbidden 格、2 个射手站低地路面）；按 rows-1-row 翻转后 4 射手全落高台、奶全在入梦砖路面
  - 传送门未实现：tile_telin/tile_telout 只渲染贴图，敌人直接穿过；查官方 route 28 检查点实为 MOVE→DISAPPEAR→WAIT→APPEAR_AT_POS→MOVE 编码；实现 telePair（检查点解析）+ applyRouteTeleport（入口进度→出口后检查点折线 toPath，含终点蓝门）+ teleEnterTime；兔洞格排除避免与随机出口双处理
  - 入梦砖：tile_sleep_road 无官方贴图（本地/参考站/素材仓库均未找到），暂用独立蓝灰 0x3a4060 区分；睡眠机制未模拟
  - playwright 验证：11 单位位置正确、route 28 敌人 dt30 在出口路线 (5,6)、TO-EX-2 兔洞 82 不受影响（teleEnemies=0）、0-1 无回归
  - 补丁（用户反馈「很多中立单位不在格子中心」）：像素级核对确认单位在正确格子且世界坐标居中，视觉偏移来自脚底贴地抬升+3/4 相机透视（高台射手最明显）；调整：抬升幅度 0.7 压低视觉中心（奶类 17→13px、高台射手 30→21~29px）
  - 补丁2（用户反馈「模型尺寸变小了」）：上一轮误把精灵宽度压到 0.95 格；已恢复原始尺寸（19.2/anim.scale，1.23~1.25 格），仅保留抬升 0.7 的居中调整，已同步 V0.29
- **传送门隐藏/出现 + 入梦砖贴图**（Codex，V0.29 草稿）：
  - 用户反馈「进入传送门后模型应隐藏再从另一边出来」；对照参考站代码：DISAPPEAR → disappear(1) 隐藏、APPEAR_AT_POS → appearAt(position) 出现，入口罚站发生在隐藏期间；map-sim 新增 teleArriveTime（到入口，不含入口罚站）/teleEnterTime（出现时间），updateEnemyPositions 在 [arrive, appear) 期间 hidden 隐藏、appear 后在出口出现继续走；验证 route 28：dt19.5~26 隐藏、dt27.8 在出口 (7,6) 出现、dt30 继续前进
  - 用户反馈「地面入梦砖没有贴图」；参考站代码明确 `tile_sleep_road = tile_sleep_wall`（官方图集无独立入梦砖路面贴图）；TILE_TEXTURES 增加 tile_sleep_road→tile_sleep_wall 映射（本地 images/map/tile_sleep_wall.png），基础色保留蓝灰 0x3a4060
  - playwright 回归：TO-EX-2 兔洞 82 不受影响、0-1 无回归、无报错
- **中立单位尺寸按参考站推导**（Codex，V0.29 草稿）：用户反馈「按官方数据和参考站数据来」；挖出参考站代码：敌人网格 `meshContainer.scale.set(z1)`（z1 默认 0.9），道具 `initSpine` 不乘任何缩放（1.0）→ 道具 = 敌人公式 ÷ 0.9 = 19.2/anim.scale/0.9（精灵 1.37~1.39 格、身体约 0.76 格），撤销此前 ×1.35 猜测；抬升 0.7 居中保留；playwright 验证 11 单位渲染正常、无报错、无上下文丢失，已同步 V0.29
- **敌人罚站倒计时**（Codex，V0.29 草稿）：用户要求增加按钮显示/隐藏敌人罚站倒计时；参考站有同款「显示等待时间」（DOM 标签 + CountDownVisible 全局开关，默认开启）；模拟器实现：工具栏新增「罚站倒计时」按钮（默认关闭），#mapWrap 内加 #waitLabels DOM 覆盖层，enemyWaitRemaining 按 path.waits 时间轴计算剩余秒数，updateWaitCountdowns 每帧投影敌人头顶位置更新标签（≥10s 整数、<10s 一位小数、<5s 变绿变小），关闭即清空；仅显示可见敌人（传送门/兔洞隐藏期不显示）；playwright 验证 route119 等罚站敌人标签 46s/86s/116s/6.9s 等实时显示、开关正常、无报错，已同步 V0.29
- **模型渲染改参考站同款「纸片贴地图」**（Codex，V0.29 草稿）：用户提出「影子才是敌人本体，上边模型只是动画」的猜想；实测确认广告牌精灵被脚底抬升后身体与影子/格心错开 3~11px；将敌人/道具从 Sprite 改为平面网格（makeFlatModel：PlaneGeometry + translate(0,0.5,0) 底部枢轴 + MeshBasicMaterial + DoubleSide），位置公式统一改 modelBottomY（脚底锚定格面）；参考站 MAP_ROTATION=0.5，但模拟器相机俯仰 -57.5° 更陡，0.5 会让平面侧对相机只剩 5px 一条缝，故用 -0.5（面向相机、后仰 28.65°）；readPixels 验证模型中心像素随显隐变化、0-1/默祷圣祠/TO-EX-2 全部正常渲染无报错，已同步 V0.29
