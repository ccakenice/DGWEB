项目：DGWEB
版本：V0.24
日期：2026-08-07
状态：草稿
来源：V0.23（已定格）

改动说明（修复自动构建覆盖模式数据）：
1. 根因：.github/workflows/daily.yml 每日定时任务运行 _build_modes.py + _gen_mode_pages.py，
   旧版脚本把线上 modes.json 从 12 模式减为 9（缺集成战略/保全派驻/生息演算），
   并覆盖 9 个模式详情页为精简模板版（删 3448 行）。
2. 修复 _build_modes.py：
   - MODES 补齐 3 个缺失模式（集成战略含 year 字段 / 保全派驻 / 生息演算）
   - 集成战略 items 处理 year 保留（it.get 容错缺 code）
   - 新增保护逻辑：modes.json 已存在（手工维护含完整 stages 聚合）则跳过自动重建
3. 修复 _gen_mode_pages.py：
   - PAGES 补全 12 模式（含 3 个手工模式页）
   - 新增保护逻辑：mode-*.html 已存在则跳过覆盖（避免覆盖手工详情页）
4. 恢复被 3a0a4a2 破坏的文件（从 d79c925 检出）：
   - modes.json（12 模式，714 条 stages 聚合）
   - 9 个 mode-*.html 完整详情页（恢复 3431 行）
5. 验证：线上 dgwebq.pages.dev modes.html 渲染 12 模式卡片，
   3 个详情页（integrated-strategies / stationary-security / reclamation-algorithm）200 完整
6. 提交：79a3ad3 / 02e38a3 / ba092aa / d1b2651

修复 weserv 代理 SSL 错误（net::ERR_SSL_PROTOCOL_ERROR）：
7. index.html / search.html 的 proxyImage() 不再对 hdslb 封面生成 weserv.nl 代理 URL，
   直接保留原始 URL → img-progressive.js 命中 manifest 命中本地 ext 高清（含 LQIP 低清占位）
8. 验证：本地 Playwright index 512 张全 LQIP、weserv 请求 0；search 搜索后 350/352 张 LQIP、weserv 0
9. 提交：36a2663

修复 PV 直链代理 SSL/HTTP2 错误：
10. 根因：V0.22 官方先导 PV 用 /pv?url= 直链让 CF Pages 代抓 B 站视频流(m4s)，
    B 站校验 IP/时效签名 + CF 侧 HTTP2 握手失败 → 线上 403 / ERR_HTTP2_PROTOCOL_ERROR / 520
11. 修复：index.html 的 pvRender 弃用 video 直链，统一走官方 iframe 播放器
    player.bilibili.com/player.html?bvid=..&muted=1&t=<随机起点>（静音+随机时刻循环），
    删除 /pv 请求逻辑
12. 验证：本地 Playwright pvFrame src=player.bilibili.com 正常、console error=0
13. 提交：a6bd35d

修复模式入口缓存回退为 9 卡片：
14. 根因：modes.html/mode-*.html 的 fetchJSON 用 localStorage key 'dgdata_modesjson'
    缓存 modes.json 达 3 小时，旧版本(修复前)写入的 9 模式数据在 TTL 内命中，
    页面直接渲染旧 9 卡片，不做网络请求 → 线上明明是 12 模式却被渲染 9
15. 修复：缓存 key 增加版本号 dgdata_v12_（旧 key 一律作废）+ TTL 从 3h 缩短为 10min
16. 验证：Playwright 干净渲染 11 网格卡 + 1 featured 卡（=12 模式）、旧 9 模式 key 不再读取
17. 提交：637dc73

修复白昼模式 PV 黑块遮挡：
18. 根因：.pv-stage/.pv-frame iframe 背景写死 #000，且 pvRender 设 12s 强制
    显示兜底（不等 iframe 真正 load）→ 白昼下视频未加载完先露出黑块盖住白底
19. 修复：stage/iframe 背景改 transparent（透出主题背景，白昼显白/黑夜显黑），
    移除 12s 强制显示，仅由 iframe load 事件触发 show
20. 验证：Playwright 白昼未 ready 时 stage bg=rgba(0,0,0,0)，无黑块
21. 提交：750cb85

文件清单：
修改：_build_modes.py、_gen_mode_pages.py、modes.json、9 个 mode-*.html、AI_PROGRESS.md
其余同 V0.23（V0.23 内容 + 模式修复）